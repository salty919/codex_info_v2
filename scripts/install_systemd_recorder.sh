#!/usr/bin/env bash
set -euo pipefail

# Retained only as a diagnostic compatibility stub.  It is intentionally
# incapable of installing, replacing, starting, or removing a binary/unit.
# Publication and control belong to the persistent bundle installer under
# ~/.local/libexec.
echo 'install_systemd_recorder.sh is retired; use the verified Linux bundle installer' >&2
exit 2
