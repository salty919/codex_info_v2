# 要求管理台帳

この台帳にない振る舞いは、実装・評価・PRの対象にしない。要求を追加・変更した場合は、先にここへ安定ID、境界、失敗動作、独立オラクルを登録する。`verified`以外の行を残したまま最終ゲートを通過させてはならない。

## Issue #138 — bounded Session収集とdurable累計正本（2026-09-04）

- 目的: 1 cycleのSession入力をlatest 2GiB以下に保ったまま、同じaccount・同じusage periodで確定済みのモデル別累計を失わず、検証済みの新規rangeだけをexactly once加算してMain・概算・history latestを同じ累計正本へ揃える。
- 非目的: 2GiB上限の緩和、定常cycleでのSession全文再走査、履歴最大値またはcomponent別maxの採用、UI clamp、既存history/Sessionの書換え、Windows固有計算の追加。既往障害の一回限り復旧だけはlatest 2GiB以内を再生し、永続checkpointとlegacy境界のモデル別token/価格合計へ完全一致する場合に限ってcomponent baselineを採用する。
- 不変条件: durable累計とrange checkpointは同じaccount partition・stable period・collector generationに結合し、rolling `reset_at` driftでは継続、確認済みの真のrolloverだけで新期間を開始する。安全な加算を証明できないcycleは縮退値をcommitせずlast-good rootを保持する。
- 依存DAG: account/storage admission → stable period identity → durable cumulative/checkpoint → bounded selected ranges → atomic totals/history commit → immutable details root → Linux/Windows表示。
- raw clause対応: 2GiB超でも常識的な資源量で全件累計を保持→`CUM-138-01`、古いSessionを再読込せず記録済みrangeだけ反映→`CUM-138-02`、reset drift/真のrollover→`CUM-138-03`、API・Main・Graph・価格一致→`CUM-138-04`、既存データ保護と実機復旧→`CUM-138-05`、矛盾しない実線・破線・confirmed gap表示→`CUM-138-06`、remote/local/DB障害中の記録継続と復旧→`CUM-138-07`。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| CUM-138-01 | 同一stable periodのdurableモデル別token/価格累計をaccount DBの正本として保持し、Session inventoryが2GiBを超えてselected prefixから既記録fileが外れても累計を減らさない | selected prefixだけの再集計値でdurable累計を全置換しない。2GiBは1 cycleの入力上限であり累計母集団ではない | `src/main.rs`、`src/usage_store.rs` | durable baseline＋2GiB overflow＋verified tail固定caseでbaseline保持とtail 1回加算 | implemented |
| CUM-138-02 | source identityとcheckpointで検証した未記録rangeだけを累計へexactly once加算し、range cursor・累計・history sampleを一transactionでcommitする | restart、同じcycle再実行、selected集合変更で二重加算しない。identity/generation不一致、partial tail、変更prefixは加算せずlast-good保持 | `src/main.rs`、`src/usage_store.rs` | 同じrangeのrepeat/restart、変更prefix、stale generation固定caseで非重複・非mutation | implemented |
| CUM-138-03 | rolling `reset_at`の揺れをstable period内として累計継続し、quota回復または期間境界の観測で確認した真のrolloverだけ旧累計を新periodへcarryしない | `reset_at/window_seconds`の単純な完全不一致で0起点にしない。境界不明なら新periodを推測せずlast-good保持 | `src/main.rs`、`src/usage_store.rs` | same-period drift＋checkpoint＋restartとconfirmed rolloverの固定case | implemented |
| CUM-138-04 | `models`、`estimated_cost_label`、current `history` sampleを同じaccepted cumulative rootから同じdata generationとしてcommit・publishする | UI、REST projection、history最大値からの再計算・mergeを禁止し、不完全cycleで一部surfaceだけ更新しない | `src/main.rs`、`src/usage_store.rs` | 1 details root内のモデルtoken/価格合計とhistory latestのexact一致、失敗cycleのroot不変 | implemented |
| CUM-138-05 | 修正・live復旧・update・restartで既存history、Session、profile sentinelを変更せず、1 cycleのselected Session bytesを2GiB以下に保つ | exactな復元根拠なしに過去最大値をcurrentへ設定せず、実Linux APIと実Windows/Linux UIが未確認なら最終合格にしない | product data path、実Linux daemon/API、Linux/Windows UI | 修正前後sentinel digest、selected byte count、同一candidateの実API/UI read-back | implemented |
| CUM-138-06 | 累積vector後退はwhole-vector欠測とし、回復までcomponent別maxを表示しない。連続確定観測だけを実線、両端のある通常欠測・回復とモデル根拠のないquota低下を破線、confirmed gapを線の切断とgap帯で示す | 欠測中のflat値、回復時刻の垂直増減、片端だけのlocal model延長、confirmed gap内の補間を作らない。remote quotaの実測時刻はlocal欠測を理由に移動・破棄しない | `src/main.rs` graph projection、Windows Graphing/ViewModel/Control | 2026-09-03実障害形状（$176→$87台→$184、85→81%）、open local gap、unattributed quota、confirmed gap固定caseをX/Windowsで独立検証 | implemented |
| CUM-138-07 | remote quota transport障害でもlocal collectorとrecorderを既存60秒周期で継続し、local-only累積をquota NULLでdurable化する。local取得失敗時も同cycleのfresh remote quotaを観測時刻と直前durable model累計で保存待ちにし、daemonを止めず次周期で復旧する | stale quotaを新timestampへ複製しない。local失敗時は実collector世代とaccount admissionが揃う場合だけ保存し、fresh local観測とは扱わずsingle-flightを解放する。DB失敗はexact pending batchを保持し成功まで新batchで上書きしない | resident scheduler、local collector、recorder、usage store | transport error→local-only pending row/quota NULL、local error→fresh quota＋durable model pending row、stale admission拒否、次周期single-flight、DB exact batch retry、live DB timestamp/generation進行 | implemented |

## Issue #129 — account別usage storageとSession帰属（2026-09-02）

- 目的: 同じOS user・同じ`CODEX_HOME`でChatGPT accountを切り替えても、usage DB、writer、backup、checkpoint、Session range、REST/UI rootを混合・上書きしない。
- 非目的: legacy DB/Sessionの推測帰属・移行・削除、Windows固有の第二実装、launcher/package/update/Release変更。
- 不変条件: current data ownerは`(ProfileScopeId,AccountScopeId,StorageEpoch)`で一意、raw email/AccountKey/tokenは永続面とRESTへ0件、identity boundaryでは旧durable dataを保持しpublic rootだけを空へ切り替える。
- 依存DAG: safe auth/data root → confirmed AccountKey window → profile registry/HMAC scope → physical account DB → Session baseline/range → atomic transaction → current memory root → REST/UI。
- raw clause対応: Issue #129 AI1..AI5→`AUTH-129`、AI6..AI10→`GEN-129`/`DB-129`/`REST-129`、AI11..AI15→`SESSION-129`、AI16..AI18→`LEGACY-129`、finite oracle 1..10→`EVIDENCE-129`。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| AUTH-129 | owner-only・stable・boundedな`auth.json.tokens.account_id`と前後`account/read`、同一processの`account/updated` counterが全て一致したwindowだけをcanonical AccountKey authorityにする。install keyをHMAC key、exact AccountKey bytesをdomain-separated messageとして64 lowercase hex AccountScopeIdを作る | UID/mode/symlink/size/duplicate key/TOCTOU/schema/generation/前後値のどれかが不正ならraw値を保持・logせずidentityを失効し、DB mutationとusage publishを0件にする | `src/account_scope.rs`、`src/protocol_contract.rs`、`src/main.rs` | `account_key_contract_rejects_missing_types_controls_limits_and_symlinks`、`account_authority_rejects_a_same_size_mid_read_replacement`、`account_update_generation_is_local_strict_and_permanently_invalidated`、`hmac_scopes_are_stable_and_account_separated`、`raw_identity_canaries_are_absent_from_partition_paths_metadata_and_candidate_db` | verified |
| GEN-129 | StorageEpoch、auth_epoch、AccountUpdateGeneration、CollectorEpoch、CycleSeq、DataGenerationを別namespaceとし、完全なadmission tuple一致だけをcommit/publishする。scope→epoch registryは同一accountで再利用する | metadata欠損・不一致、unknown artifact、initialized DB欠損、counter overflowはrecovery-required。新epochや旧account fallbackで隠さない | `src/account_scope.rs`、`src/main.rs`、`src/daemon.rs` | `profile_registry_reuses_epoch_and_separates_physical_paths`、`registry_rejects_orphans_unknown_artifacts_and_missing_initialized_database`、`auth_epoch_overflow_requires_process_recovery_and_cannot_resurrect_usage`、`seven_account_switch_crash_images_restart_without_old_account_fallback` | verified |
| DB-129 | accountごとに`history/accounts/v1/<scope>/epoch-<epoch>/usage_history.sqlite3`を物理分離し、exact oneのpartition row、account lock、backup、checkpoint、recorded marker、gap ledgerを同directory/DBに所有させる | wrong path、partition row 0/複数、version/scope/epoch不一致、quick_check失敗、foreign candidateはrejectし、旧DBをcurrentへ戻さない | `src/account_scope.rs`、`src/usage_store.rs`、`src/daemon.rs`、`src/main.rs` | `account_partitions_isolate_same_keys_metadata_backups_and_gap_ledgers`、`wrong_partition_schema_is_rejected_by_a_read_only_probe`、`recorder_writer_persists_only_the_caller_generation_and_holds_the_profile_lock`、`allocated_candidate_and_renamed_final_resume_without_a_stale_writer_lock`、7点crash test | verified |
| SESSION-129 | identity boundaryの既存fileはEOFをbaselineにし、同一CollectorEpoch内のverified rangeだけをusage、dedupe、checkpoint、DataGeneration、markerと同一transactionでcommitする | partial tailは次LFまで捨てる。rotation/truncation/fingerprint/record errorはfile candidate、root/I/O/admission/post-scan identity/DB errorはbatch全体をreject。baseline/legacy/partial sourceは削除しない | `src/main.rs`、`src/usage_store.rs`、`src/daemon.rs` | `account_boundary_baselines_existing_sessions_and_only_commits_verified_appends`、`post_scan_identity_mismatch_discards_rows_cursors_markers_and_publication`、`changed_session_prefix_is_freshly_baselined_without_cursor_or_marker_reuse`、`session_range_checkpoint_marker_and_generation_commit_atomically`、`recorded_overflow_cleanup_deletes_only_fresh_authorized_files`、`session_traversal_budgets_and_symlink_rejection_have_exact_boundaries`、`aggregate_overflow_collects_latest_prefix_and_fatal_selected_file_has_no_fallback` | verified |
| REST-129 | logoutは`auth_required`、identity/storage failureは`error`、switch収集中は`initializing`のHTTP 200 strict empty details rootを新generationでpublishする | identity boundaryだけがlast-good clearの例外。同じconfirmed identityのtransport/quota/local一時失敗とstale completionはcurrent rootを保持する | `src/main.rs`、`src/server.rs`、`docs/REST_API_V1.md` | `public_snapshot_is_whitelisted_and_tracks_auth_state`、`post_scan_identity_mismatch_discards_rows_cursors_markers_and_publication`、`seven_account_switch_crash_images_restart_without_old_account_fallback`、`stale_thread_and_local_results_are_complete_no_ops`、`server::tests::published_pair_generation_matches_fixed_vectors` | verified |
| LEGACY-129 | legacy `history/usage_history.sqlite3`、backup、Session、settingsをcurrent accountへ読まず、移動・rename・write・deleteしない | legacy存在だけではaccount registry作成を妨げないが、legacy row projectionは0件。推測migrationは行わない | `src/main.rs`、`src/account_scope.rs`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md` | `legacy_database_backups_and_sessions_remain_byte_identical`、account partition loadによるlegacy projection 0、production `migrate_verified` caller 0件 | verified |
| EVIDENCE-129 | Issue #129 finite oracle 1..10を最新revisionで実行し、fmt、clippy all-targets、full locked tests、fresh独立監査を全てPASSさせる | FAIL、INCONCLUSIVE、未実行、test 0件が1つでもあればpush/PR/status:reviewを禁止する | 上記owned scope | 条項別command evidenceとfresh evaluatorの逆引き監査 | verified |

## Windows版をX版品質へ揃える（2026-08-27）

- 目的: resident serviceをquota/local/historyの唯一のauthority/writerとし、Linux / WindowsのMain、Graph、Threadsをstrict validation済み単一`/v1/details` generationへ揃える。
- 非目的: X版のレイアウトを無条件に複製すること、未取得値の推測、実Windows未確認項目のPASS化、PR・Releaseを要求完成前に開始すること。
- 不変条件: 同一fixtureの値・期間・欠測・残量・thread意味論はplatform間で一致する。UIはSQLite/JSONL/app-serverを再収集せず、値の再計算・mergeを行わない。部分世代、stale completion、失敗応答はlast-good rootとraw SQLiteを壊さない。
- 依存DAG: resident collection → HistoryCanonicalizer/storage → immutable details generation → strict details validation → Linux / Windows Main/Graph/Threads projection → 実環境評価 → 最終判定。
- raw clause対応: 「resident serviceが唯一authority/writer、single details root、health readiness、control-only auth」→ WIN-PARITY-DATA/WIRE-01/STATE/PAIR-01/CTA-01、「HistoryCanonicalizer、値形状除外撤廃、raw SQLite非破壊」→ WIN-PARITY-HISTORY-01、「shared rolloverと既存回帰を同一revision」→ WIN-PARITY-EVIDENCE-01。「Windows側の品質をX版にまで」→ WIN-PARITY-DATA/STATE/UX/OPS、「計画→分析→実装設計→TDD→連結評価→結果→判断→終了」→ PROC-WIN-PHASE-01、「Luna-maxをユニット単位、SOLは計画・設計・最終評価」→ PROC-WIN-DELEGATE-01、「`.com/{UUID}_req.md`/`_res.md`」→ PROC-WIN-HANDOFF-01、「1分周期、待機中は状態通知以外を読まない」→ PROC-WIN-MONITOR-01、「未完なら再計画して反復」→ PROC-WIN-CLOSURE-01。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| WIN-PARITY-DATA | Linux / WindowsのMain/Graph/Threadsは同じstrict validation済み単一`/v1/details` generationからquota、model usage、履歴、欠測、未使用帯、threadを表示する | UI側再収集・値再計算・別endpoint/row mergeを禁止し、失敗時は全surfaceで同じlast-good rootを保持する | resident SnapshotPublisher、Linux UI、Windows Core/ViewModels/Graphing | shared rollover `100% / $1 → 41% / $323.674247`、`graph_delayed_quota`、同一revisionのX/Windows表示 | implemented |
| WIN-PARITY-WIRE-01 | `/v1/details`は`history_gaps`を含む正本のexact 13 top-level keyとexactly oneの正規generation headerだけを一つのatomic rootとして受理する | 欠落・未知・重複key、contract revision/header不正、部分採用はcandidate全体rejectとlast-good保持 | `src/server.rs`、Linux/Windows details parser/domain | exact valid fixture、各1-key欠落/追加/重複、header不正、旧root保持unit | implemented |
| WIN-PARITY-HISTORY-01 | `HistoryCanonicalizer`は同じprofile-owned DB read内で、quota観測のない重複backfill reset群と継続cycle内だけのreset断片をperiod authorityから外し、既存`timestamp/reset_at`から同一cycleと検証できたminuteについてdistinct non-null quota<=1かつ既存componentwise-dominant cumulative vector値がある場合だけ1 logical sampleを作る。実resetを分へ丸めた旧cycle末尾・新cycle先頭の共有bucketは、旧cycleがそこで終了し新cycleだけが後続分へ継続すると確認できる場合だけ新cycleへ所属させる | quota競合、dominant不存在、非比較は該当minuteだけを公開対象外にし、別cycle間の曖昧な所属は候補全体rejectとする。100%・7日窓など数値の形で除外せず、merge/max/last/null化せずraw SQLiteをrewrite/deleteしない | resident collector/canonicalizer/`src/usage_store.rs`、REST projection、Linux/Windows sink | conflict/comparable/incomparable/共有境界/ambiguous boundary/shadow reset/duplicate固定fixture、`tests/fixtures/graph_weekly_reset_rollover.json`のshared rollover exact値、minute隔離/全体reject/raw row不変 | implemented |
| WIN-PARITY-CURRENT-01 | current periodの`end_at`は観測時刻までの有効端`min(reset_at, observed_at)`、`reset_at`はsample所属用canonical境界として別に保持する | 将来resetまで横軸を伸ばす、`end_at`をreset境界へ流用する、過去期間の確定境界を書換えることを禁止する | Rust period projection、Windows `EffectiveGraphEnd` | X/Windows同一fixtureのcurrent/過去右端exact値 | verified |
| WIN-PARITY-FIRSTOBS-01 | 初回未観測区間はX版と同じ水平baselineを表示し、最初の実観測時刻に垂直なfirst-observation境界を置く | 未観測区間を斜線の利用量へ補間せず、idle-bandの可視色・高さ・境界を別系列の値から推測しない | 共通graph fixture、Rust/Windows projectionとrender adapter | 独立固定期待値、no-diagonal pixel/geometry検査、同revision実Windows Graph画像 | verified |
| WIN-PARITY-STATE | Linux / Windowsはexact current product versionを持つreadiness health後の初回strict detailsまで固定スピナーを表示し、定期更新・再接続・details失敗・stale completionで画面を空にせず完全なlast-good rootを保持する | 旧・欠落・異version healthをdetailsへ進めず、Linux launcherは検証済み旧ownerをcurrent binaryへ交代する。listener-only health、control応答、timeout、cancel、順序逆転、初回失敗を有限state tableで区別 | Linux launcher/service、Windows details client、ViewModels、Infrastructure、MainWindow | version一致/欠落/不一致、旧→新service交代、state-machine unit、API integration、実Windows startup/refresh/failure UIA | implemented |
| WIN-PARITY-PAIR-01 | resident serviceは完全detailsを一つのimmutable generationとしてpublishし、Linux / Windows UIは正規generation headerを持つstrict details一応答だけをroot commitする | `/v1/status`や別projectionを作らない。header/body不正・旧completion・部分応答ではlast-good保持。counter/CSPRNG/初期generation/overflow契約は維持 | `docs/REST_API_V1.md`、Rust SnapshotPublisher、Linux/Windows details parser/state root | 注入epoch固定vector、起動直後counter1、data counter2、reject/read不変、details-only atomic commit、status route不存在unit | implemented |
| WIN-PARITY-RETRY-01 | saved selectorは次回起動時だけ再接続に使い、同一generationの自動retryは0回、利用者の明示Retryは新generationを一つだけ開始する | 旧child callback、重複click、cancel後completion、supervisor child exitで同世代を再利用しない | Windows connection supervisor、Main state owner | generation ID、child起動/reap回数、stale callback廃棄を数えるfinite state test | verified |
| WIN-PARITY-UX | Windows版のMain/Setup/Settings/Graph/Threads/Legalは正本geometry、DPI、配置、drag、keyboard、focus、accessibility、single-version表示を満たす | 900×480 fixed群、Graph 700×480以上、複数monitor、text scale、high contrast、child singleton、見えない配置をPASSにしない | `windows-client/src/CodexInfo.WindowsClient/*.axaml*`、window/input/accessibility adapter、Presentation tests、E2E | UIA geometry/pixel/keyboard checks、全登録surfaceのfresh Windows capture、独立意味棚卸し | verified |
| WIN-PARITY-CTA-01 | failure surfaceのprimary CTAは状態ごとに1個とし、auth-start/auth-checkをcontrol-onlyとしてdetails rootから分離する | control応答をdata successへ変換せず、auth-start/auth-check/retry/updateを同時primary表示しない | Windows Main/Setup view-modelとAXAML | state×CTA cardinality、control後details未更新のlast-good、fresh Windows capture | implemented |
| WIN-PARITY-LEGAL-01 | Legalは固定viewport内のchapter/page表示とし、chapter position、Back、Close、Minimizeを常時操作可能にする。埋込みMarkdown正本は見出し・箇条書き・リンク・強調・inline code・code fence・HTML commentの記法を表示用通常テキストへ変換し、法的内容とURLを欠落させない | whole-text/root ScrollViewer、viewport外control、本文欠落、Markdown記号のraw表示、`.txt`/拡張子なしライセンス本文の改変を禁止する。変換不能・resource欠落時は既存fail-closed pageを表示する | Windows legal catalog/plain-text projection/window/view-model、Presentation tests、E2E | 固定Markdown→plain oracle、全埋込み`.md`の禁止記法scan、全page到達unit、900×480 UIA/text/capture意味棚卸し | verified |
| WIN-PARITY-OPS | Windows版は接続、Setup/Settings、SSH/WSL起動、再接続、更新通知、install/update/uninstallで既存設定・履歴・last-goodを破壊しない | 初回/再起動/保存失敗/取消/接続不能/更新失敗/再入/重複操作を有限transactionとして扱う | `windows-client/src/*`、installer/update tools、Windows tests/E2E | process argv test、settings persistence、installer/update transaction tests、実Windows導入/復旧証拠 | verified |
| WIN-PARITY-RECOVERY-01 | raw SSH host/userはsaved selector復旧用の明示one-session経路でだけ受け取り、通常Setupには表示せず永続化件数を0にする | settings、argv、stderr、log、履歴へraw/展開済み値を残さず、session終了後に再利用しない | Windows Setup/connection/settings/process adapter | persistence/file/log scan、process argv test、再起動後非再利用test | verified |
| WIN-PARITY-EVIDENCE-01 | shared rollover、`graph_delayed_quota`、既存history/rolling/delayed/gap/no-history/REST/Windows回帰を同じproduct revisionで確認する | test 0件、未実行、SKIP、stale artifact、X/Windows oracle不一致をPASSにせず、新workflow gate・全直積を追加しない | 既存Rust/REST/Windows test、`windows-client/tools/Run-WindowsClientE2E.ps1`、実Windows evidence | non-zero test count、同一revision identity、固定oracle、UIA/pixel/意味棚卸し | implemented |
| PROC-WIN-PHASE-01 | 作業は計画→分析→実装設計→TDD実装→連結評価→結果報告→判断→終了の順に進める | 要求・設計・対象unitが未確定の状態で実装または最終ゲートを開始しない | `.com/*_req.md`、`.com/*_res.md`、`docs/REQUIREMENTS_LEDGER.md` | phaseごとの成果物と後続開始条件の照合 | verified |
| PROC-WIN-DELEGATE-01 | SOL親が計画・設計・統合・最終評価を所有し、Luna-max子が狭い非重複unitの分析・TDD実装・連結評価を行う | 子は指示外編集・他差分の巻戻し・未実行PASSを行わない | `.com/*_req.md`、`.com/*_res.md` | req ownershipと実diff、res command evidenceの対応 | verified |
| PROC-WIN-HANDOFF-01 | 各Luna unitは`.com/{UUID}_req.md`を入力正本とし、完了時に対応する`{UUID}_res.md`へ結果を書く | res欠落、UUID不一致、指示外成果は回収済みにしない | `.com/*_req.md`、`.com/*_res.md` | UUID一対一・必須section検査 | verified |
| PROC-WIN-MONITOR-01 | SOL親はLuna作業中、1分間隔でagent status通知だけを待ち、待機中にrepo・ログ・差分を読まない | 1分未満のpoll、完了前のres存在確認、重複回収、同一入力の再実行をしない | `.com/*_res.md`、agent status | 1分wait履歴と一度だけのres回収 | verified |
| PROC-WIN-CLOSURE-01 | 全res回収後にSOLが条項別証拠を判断し、未完なら原因単位で再計画して新UUIDへ反復する | FAIL/INCONCLUSIVE、実Windows未確認、変更後の古い画像を終了扱いしない | `docs/REQUIREMENTS_LEDGER.md`、`.com/*_res.md` | 全行verified、fresh独立評価、同一revision実Windows証拠 | verified |

## raw payload CLI（2026-08-27、installed launcherはU128-28）

- 目的: service・開発・E2Eが直接使うpayload契約を、無印、`--ui`、`--port PORT`、`--stop`、`--help`だけへ限定する。
- 非目的: 顧客向けupdate/control authority、LAN公開、任意bind address、旧オプション互換を提供しない。
- 不変条件: 待受addressは常に`127.0.0.1`、同一profileのdaemon ownerは1つ、停止対象はlock identityで検証したPIDだけ、未知・重複・欠落・範囲外引数は副作用なしで拒否する。
- 依存DAG: CLI parse → loopback config → service/UI lifecycle → Windows起動引数・systemd/E2E → 多言語help。
- raw clause対応: payloadのloopback/owner/stop/help→PROC-LAUNCH/STOP/OPTIONS/HELP/I18N、installed
  launcherのstart/UI/update/status/stop/disable/removeと更新収束→U128-01..38。

### 有限受理・拒否表

| argv | 終了/動作 | 副作用契約 |
| --- | --- | --- |
| なし | daemon+REST、127.0.0.1:8787 | Windowなし、owner最大1 |
| `--port P`、P=1..65535 | daemon+REST、127.0.0.1:P | Windowなし、非loopback bindなし |
| `--ui` | daemon+REST確保後にX UI | healthy owner再利用、無ければ1つだけ開始 |
| `--ui --port P`、P=1..65535 | 127.0.0.1:Pを使う上記UI | argv順序はこの形だけ |
| `--stop` | 下記停止契約 | daemon/REST/UIを新規生成しない |
| `--help` / `--h` / `-h` | locale別help、終了0 | daemon/REST/UI/DB変更なし |
| 旧6 option、未知語、誤記、重複、欠落、余分な位置引数、逆順、`--port=P`、help/stopとの混在、P=0/65536/負数/非数値/Unicode不正 | 終了非0 | daemon/REST/UI/DB/lock変更なし |

### `--stop`停止契約

- profileのlock pathが存在しなければ停止済みとして終了0。
- lock pathが存在するが、完全なlock identity・PID starttime・executable identityを検証できない場合は、何もsignalせず終了非0。
- 検証済み所有PIDにTERMを1回だけ送り、最大5秒間、同じlock pathが消えることを待つ。SIGKILLへ昇格しない。
- signal失敗、5秒timeout、停止中に別ownerが同じlockを取得、lockが残る場合は終了非0。
- DB、backup、reset hint、source JSONLの内容と存在を変更しない。listenerだけが存在しlock ownerを証明できない場合は無関係processとしてsignalしない。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| X-START-01 | X版の初回遷移ではヘッダーと製品バージョンを固定し、readiness health後のstrict details一世代だけをatomic表示する | quota/local/history/threadを別取得・段階表示せず、control応答をrootへ混ぜない | Linux details client、`ui/app.slint`、`src/main.rs::sync_ui` | native startup details state test、X11 fresh startup visual gate | implemented |
| X-START-02 | X版は最初のstrict details generationが揃うまで内容領域にスピナーを表示する | healthや部分fieldで解除せず、完全details公開後にだけ解除 | Linux details client、`ui/app.slint` | details boundary unit、X11 startup visual gate | implemented |
| X-START-03 | X版の初回service/details失敗時はスピナーを解除し、最後の完全表示または失敗状態を表示する | 部分値・0・未取得へ黙って置換しない | Linux details client、`src/main.rs::native_startup_loading` | `native_startup_failure_releases_loading_surface`、失敗→回復details test | implemented |
| X-START-04 | X版の起動ウィンドウは主モニターの可視デスクトップ内へ配置し、利用者から見えない位置へ出さない | 別モニター・負座標・デスクトップ外の場合は合格にしない | `src/main.rs`、`run.sh`、`scripts/x11_startup_visual_gate.sh` | visible position test、X11可視範囲ゲート、実画面キャプチャ | verified |
| X-START-05 | local generationとUI payload identityを完全検証できた後のdaemon/REST接続・details失敗でもX版GUIを表示し、接続失敗と再試行手段を提示する | generation/ownerが安全でも接続失敗だけでWindowを即時終了させない。一方、generation不整合またはforeign/unknown ownerの`SAFE_BLOCKED`では未検証payload UIを実行しない | `src/main.rs`、`run.sh` | verified-local＋失敗portでの実起動保持と、unsafe-generation/foreign-ownerでUI process 0の有限2-path test | implemented |
| X-START-06 | X版はpreviewを使わない実サービス接続で、完全表示後の選択endpoint停止時に最後の完全表示を保持し、同一endpointのサービス復旧後に表示をreadyへ戻す | 認証画面をreadyと誤認せず、別endpoint・部分値・自動fallbackを成功扱いにしない | `scripts/x11_startup_visual_gate.sh`、`scripts/x11_service_recovery_visual_gate.sh` | 実resident service + isolated app-server fixtureのX11画像で、ready利用枠バー→停止/error保持→同一port復旧/readyを状態別にpixel判定 | implemented |
| X-GRAPH-01 | X版の過去グラフは期間境界・累積モデル値・未使用帯・既存の遅延残量projectionを正本どおり表示する | service/details/UIでsampleをmerge修復せず、confirmed gapを斜めの使用量へ変換しない | Linux details client、`src/main.rs` graph projection | delayed/rollover fixed fixture tests、X11 graph visual gate | implemented |
| X-GRAPH-02 | X版をquota reset後に新規起動してGraphを開いた場合、minute-start保存された境界bucketを含む現在期間を自動選択し、selector・期間ラベル・sample・全描画系列を同じcanonical resetへ揃える | 値形状で100%行を除外せず、同一cycle aliasは一つのstable period IDにし、別cycleを混在させない | service HistoryCanonicalizer、Linux details client、graph selection/projection | rollover A→B、boundary bucket、全graph unit、同一release revisionのfresh X実画面と周期refresh後再確認 | implemented |
| X-THREAD-01 | X版thread取得はCodex processがopen中のSession pathだけを15秒の共通deadline内で個別`thread/read`し、不完全でも完全な前回表示を部分値で上書きしない | 全Session `thread/list`とstate-DB-only fallbackを使わない。途中終了した非state `response_item`は隔離するが、lifecycle/model不正、ID/path/schema不一致は全体失敗 | `src/thread_contract.rs`、`src/main.rs` | `active_thread_adapter_rejects_partial_rollout_fallback`、`multiple_running_threads_are_all_published_with_stable_order`、`recoverable_rollout_parser_skips_malformed_non_state_records_only`、実Codex active-path取得 | implemented |
| WIN-START-01 | Windows版の初回起動はreadiness health後のstrict details一世代まで内容領域を隠し、固定位置のスピナーを表示する | health・control応答・部分fieldの順番描画を許可せず、失敗時は再試行を表示 | Windows details client、MainWindow/ViewModel/Presentation tests | details-only startup tests、実Windows UI Automation/E2E | implemented |
| WIN-GRAPH-01 | Windows版の過去グラフはX版と同じ期間境界・累積値・未使用帯・遅延残量projectionを表示する | accepted detailsをmerge修復せず、未観測区間を使用量へ推測せず、専用ニュートラル色を失わない | Windows details client、`windows-client/src/CodexInfo.WindowsClient/Graphing` | delayed/rollover graph projection tests、実Windows過去期間画像 | implemented |
| WIN-VERSION-01 | Windows版の製品バージョンはメイン画面に一度だけ表示し、子ウインドウのタイトルへ重複表示しない | X版と同じversion authorityを使用し、表示欠落・重複を許可しない | `Cargo.toml`、`Cargo.lock`、`windows-client/Directory.Build.props`、`windows-client/src/CodexInfo.WindowsClient/MainWindow.axaml`、`windows-client/src/CodexInfo.WindowsClient/ViewModels/MainWindowViewModel.cs` | Windows contract gate、実Windows UI Automation | verified |
| PROC-LAUNCH-01 | raw payload modeを固定し、引数なし/`--port PORT`はloopback daemon+RESTのみ、`--ui`/`--ui --port PORT`はdirect payload UIを起動する | portは1..65535、addressは127.0.0.1固定。installed profileのwriter/listenerはsystemd owner 1件だけで、顧客入口には使わない | `src/main.rs`、service、record/CLI E2E、REST/data docs | 全受理形・port境界unit test、direct payload mode別実行 | implemented |
| PROC-STOP-01 | `--stop`は同一profileのlock identityで検証した常駐daemonだけへTERMを送り、lock解放まで待つ。停止済みなら成功する | 無関係PIDを停止しない。timeout、signal失敗は非0。DB/historyを削除しない | `Cargo.toml`、`Cargo.lock`、`src/main.rs`、`src/daemon.rs`、`scripts/cli_contract_e2e.sh`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md` | isolated HOMEでstart→health→stop→lock/listener消失、停止済み冪等test | verified |
| PROC-OPTIONS-01 | raw payload引数は無印、`--ui`、`--port PORT`、`--ui --port PORT`、`--stop`、`--help`/`--h`/`-h`だけとする | `--service`、`--ui-only`、`--all`、`--listen`、`--record-daemon`、`--once`、誤記、重複、欠落を副作用なしで拒否する。launcher集合はU128-28 | `src/main.rs`、`src/i18n.rs`、payload E2E | finite accept/reject matrix、helpとparser集合一致 | verified |
| PROC-HELP-01 | `--help`/`--h`/`-h`は起動せず、既存のi18nカタログで利用可能な起動モードと動作を表示する | ヘルプ要求でdaemon・REST・GUIを生成しない、固定単一言語の製品メッセージを出さない | `src/i18n.rs`、`src/main.rs`、`run.sh`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md` | 全対応言語のcatalog test、help mode unit test、`LC_ALL`を切り替えた`run.sh --help`実行ログ | verified |
| PROC-I18N-01 | payloadとinstalled launcherの利用者向け固定メッセージは、本体の対応locale契約から導出する | 単一言語の製品文言を複数入口へ矛盾して複製しない | `src/i18n.rs`、`src/main.rs`、`run.sh`、product docs | 全対応言語help、`LC_ALL`切替launcher/payload実行 | verified |
| PROC-LEDGER-01 | 要求台帳にない要求・CI失敗条件を実装、評価、PRへ含めず、local入口はschemaと実装状態、完了時の`--final`はremoteを含むverified状態を確認する | PRでしか得られない証拠をPR前に要求せず、台帳行追加後は旧証拠を無効化する。Inno取得stepは固定された5 failure contract以外と第二version oracleを拒否する | `run.sh`、`.github/workflows/rust.yml`、`.github/workflows/windows-client.yml`、`.github/workflows/release.yml`、`.github/workflows/selective-quality.yml`、`docs/PRODUCT_REQUIREMENTS.md`、`docs/REGRESSION_PREVENTION_POLICY.md`、`scripts/regression_guard.sh`、`scripts/windows_client_contract_gate.sh`、`scripts/x11_graph_visual_gate.sh`、`scripts/requirements_ledger_gate.sh`、`scripts/workflow_inno_acquisition_gate.py`、`docs/REQUIREMENTS_LEDGER.md` | local schema gate、Inno allowed/extra/second-oracle mutation、remote証拠取得後のfinal gate | implemented |
| WF-BINARY-IMPACT-01 | `main`向けsame-repository PRの完全Git差分をbranch名に依存せず有限ownerへ分類し、binary impactとCodeQL言語を同じ選択から導出する | callerがrename/copy検出を明示して両端とdeleteを渡し、空差分・未知pathは失敗する。各event内でclassifierは1回だけ使用する | `AGENTS.md`、`scripts/ci_change_scope.py`、`scripts/test_ci_change_scope.py`、`.github/workflows/*` | tracked全path、実Git copy、add/delete/rename/unknown/空差分、actionlint | implemented |
| WF-FEAT-SELECTIVE-01 | `feat/next`向けPRは任意same-repository headの完全差分を分類し、選択owner・CodeQL・distributionの実jobだけをadvisoryに評価する | 分類不能と実job失敗は赤のまま表示する。`selected-quality`はrunner上で実行せず、`feat-acceptance`、version・candidate・Release・custom check mutationは0件とし、workflow結果でmergeを禁止しない | `.github/workflows/feat-integration.yml`、`.github/workflows/selective-quality.yml`、分類器 | owner全31組合せ、異常差分、実PR job graph、ruleset/protection read-back | implemented |
| WF-QUALITY-ONCE-01 | 各eventで分類器を最大1回使用し、H0 runが生成したH1を含むfinal headでselected ownerとCodeQL言語だけを各1回実行する | mainのRelease結果oracleだけtrusted base版gateを使う。featは実job結果をそのまま表示する。event時／開始時draft、開始時closed、開始時stale、正規生成H1 observerはowner 0回、identityのない手動commitとH2は評価する | `.github/workflows/*`、`scripts/selected_quality_gate.py`、`scripts/test_selected_quality_gate.py`、`scripts/workflow_quality_gate.py`、`scripts/test_codeql_workflow.py`、`scripts/windows_client_contract_gate.sh` | owner全31組合せ、PR側oracle改変拒否、各異常result、draft/stale cancel、closed observer、H0→H1 observer→H2、手動version | implemented |
| WF-NONBLOCKING-QUALITY-01 | feat/mainのworkflow失敗をbranch mergeのrequired statusにせず、mainの`version-prepared`・`acceptance`はRelease品質と公開可否だけを判定する | ruleset・classic protectionでrequired checkを有効化せず、生成H1へのcustom check登録、poll、retry、URL照合、branch allowlistを追加しない | `.github/workflows/version-prepare.yml`、`.github/workflows/feat-integration.yml`、repository rules | actionlint、local acceptance成功/異常、custom check mutation 0、remote ruleset/protection/check-runs | implemented |
| WF-POSTMERGE-01 | merged closedとMain Quality completedの両順序でfinal-head全non-observer attemptを解決し、failure barrierがなくWindows successかつown candidate exact 1の場合だけ公開する | same-head後続successで過去failureを隠さない。Windows skippedはcandidate 0、欠落・期限切れ・複数・identity不一致は拒否し、旧成功fallback、PR再分類、品質再実行を行わない | `.github/workflows/release.yml`、`.github/workflows/windows-client.yml` | 両event順序、draft/stale/generated observer、same-head failure→success、Windows skip/success、candidate欠落/期限切れ/複数、実artifact transfer | implemented |
| WF-OVERQUALITY-REMOVE-01 | 汎用custom-check reporter、H1 custom check登録、featの`selected-quality`実行と`feat-acceptance`、poll/retry、証拠専用artifact、二段acceptanceをworkflow authorityから除去する | 実owner test・CodeQL・distributionとmain Release結果集約は維持し、旧reporter/gate/resolver群と同等fallbackを追加しない。公開lock取得後の単回authority/state再取得は二信号raceだけを判定し、汎用readbackへ拡張しない | workflow、旧script不存在、`docs/PRODUCT_REQUIREMENTS.md`、`docs/REGRESSION_PREVENTION_POLICY.md`、`docs/REQUIREMENTS_LEDGER.md` | repository実行参照0件、追加check/artifact 0件、actionlint、local workflow gate、実job時間 | implemented |
| VER-AUTO-PATCH-01 | バイナリ影響ありPRだけ、品質確認前に共有versionのpatchを十進整数でちょうど1増やす。バイナリ影響なしPRはversion 3ファイルを変更しない | patchからminorへcarryしない。期待元version不一致、malformed、同期不一致、分類処理失敗では3ファイル無変更で停止する | `.github/workflows/version-prepare.yml`、`.github/workflows/windows-client.yml`、`scripts/ci_change_scope.py`、`scripts/product_version.py`、`Cargo.toml`、root `Cargo.lock`、`windows-client/Directory.Build.props` | patch境界fixture、PR #30 no-binary-impact、version mutation 0、byte不変検査 | implemented |
| VER-SERIES-FIXED-01 | 自動採番はmajor/minorを変更せず、両値は利用者の明示指示を伴う別変更でだけ更新する | 通常自動処理のcarry、推測、既定値置換を禁止する | `.github/workflows/version-prepare.yml`、`.github/workflows/windows-client.yml`、`scripts/product_version.py`、`scripts/test_product_version.py` | major/minor不変fixture、通常PRの無断series変更fail-closed検査 | verified |
| WF-SERIAL-01 | version更新はPR単位、Release公開はversion tag単位で直列化する | concurrent head更新はnon-force pushを失敗させ、retry・上書きしない。Releaseはlock取得後の完全不存在だけ作成し、orphan/Draft/partial/mismatchを修復しない | `.github/workflows/version-prepare.yml`、`.github/workflows/release.yml` | stale head、push拒否、同tag二信号、別tag並行、orphan/Draft/partial/mismatch | implemented |
| SEC-CI-TRUST-01 | contents/Release write jobはtrusted baseまたはtrusted run outputだけを実行し、PR headをGit object dataとして読む。custom check writerは置かず、Release jobはsourceをcheckoutしない | fork、不正version、競合push、producer/candidate欠落ではmutationを開始または継続しない | version・release workflow、local trust gate | actionlint、untrusted checkout検査、non-fast-forward fixture、custom check mutation不存在 | implemented |
| SEC-CODEQL-LOG-01 | protocol検証失敗を診断してもtoken、authorization、password、private key、raw endpointをcleartext logへ渡さない | failure時も固定非秘密markerだけを出し、秘密値の補間・Debug表示・長さによる復元可能情報を禁止 | `src/protocol_contract.rs`のCodeQL指摘経路 | alerts #2-#5 dataflow、対象unit、CodeQL再解析 | verified |
| SEC-RELEASE-CODEQL-01 | owner選択から導出したCodeQL言語だけをH1でexact 1回実行し、mainのRelease品質では`acceptance`・`version-prepared`とanalysis成功、errorおよびcritical/high finding不在を公開条件とする | selected言語のpending/missing/failure、余分な言語実行、alert dismiss、古いheadではRelease公開不可。失敗はworkflowへ表示するがmergeを禁止せず、Windows client/acceptanceでCodeQLを再実行しない | `.github/workflows/codeql.yml`、`.github/workflows/selective-quality.yml`、`scripts/test_codeql_workflow.py`、`scripts/selected_quality_gate.py`、`scripts/workflow_quality_gate.py` | owner→actions/csharp/python/rust全写像、selected/nonselected結果mutation、PR #67/#68相当、実CodeQL check | implemented |
| SEC-CODEX-REVIEW-01 | 変更が確定したPR最新headに`@codex review`を1回だけ投稿し、同headのCodex reviewを補助レビューとして使用する | 新commit後の旧review、non-outdatedかつ未解決のP0/P1、review未完了ではready/releaseをHOLDする。独自API key workflowやrequired statusを追加せず、CodeQL・acceptance・必要な承認を代替しない | Codex GitHub Code review setting、PR comment/review thread、`docs/PRODUCT_REQUIREMENTS.md`、`docs/REGRESSION_PREVENTION_POLICY.md` | comment時head SHAとreview commit SHAの一致、GraphQL review threadのoutdated/resolved/severity監査、workflow secret/action scan | verified |

## Linux core bundle（2026-09-01、Issue #128で収束契約を強化）

- 目的: 既存の`windows-vX.Y.Z` Releaseへ、同じstable version・final source SHAに結び付くLinux core bundleを同居させ、repository cloneやCargoなしでuser-systemdへ導入し、launcher・起動前reconcile・1時間timerを同じauthorityへ収束させる。
- 非目的: system-wide root install、apt/rpm repository、container image、別Linux tag/channel、未指定のdistribution・architecture・glibc・systemd・署名・publisher対応の表明。
- 不変条件: targetは`x86_64-unknown-linux-gnu`に固定し、互換性の唯一の入力はmanifestの実測`glibc_minimum`である。archive、checksum、外部/内部manifest、byte-identicalな`run.sh`、payload、unit、persistent installer、license/noticeを一つのcandidate identityへ結び付ける。更新確認は固定repositoryの公開Releaseだけを読み、anti-downgradeを守り、同versionでもlocal不整合をverified repairする。
- 依存DAG: Release authority → exact archive/generation identity → single resolver/installer → atomic journal/current switch → managed activation → source-bound health → recorder identity → strict details readiness → A/B terminal → recorder commit/gap publication。
- この節の状態列は文書・ローカル契約の実装状態を表す。公開Release/remote read-back/実systemdの外部観測を推測して`verified`へ変更しない。
- raw clause対応: 旧bundle要件は下表の`LINUX-BUNDLE-*`、Issue #128の更新デッドロック解消、起動入口、常駐記録、欠測・Session保持、有限回復、同version修復は後続`U128-01..38`を正本とする。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| LINUX-BUNDLE-TARGET-01 | Linux coreの配布targetを`x86_64-unknown-linux-gnu`に固定し、互換性はbundle manifestの実測glibc minimumだけから判定する | manifestのtargetまたは実測minimumが欠落・不一致ならcandidateを拒否し、他distribution/architecture、署名済み、publisher検証済みを表明しない | `scripts/build_linux_bundle.sh`、`scripts/test_linux_bundle.sh`、`.github/workflows/linux-distribution.yml`、`docs/PRODUCT_REQUIREMENTS.md` | targetとglibc minimumの固定manifest、欠落・不一致のfail-closed fixture | implemented |
| LINUX-BUNDLE-RELEASE-01 | Linux archive、SHA-256 checksum、manifestを同じversion・final source SHAの既存`windows-vX.Y.Z` ReleaseへWindows assetと同居させる。main向けrelease candidateではLinux-only差分にもWindows評価/candidateを追加し、通常`feat/next`選択は維持する | version、source SHA、target、size、digest、asset名の欠落・複数・不一致、WindowsまたはLinux candidateの欠落、partial/orphan Releaseは公開しない | `.github/workflows/version-prepare.yml`、`scripts/ci_change_scope.py`、`.github/workflows/linux-distribution.yml`、`.github/workflows/selective-quality.yml`、`.github/workflows/release.yml`、`scripts/workflow_quality_gate.py`、`tests/release_linux_rollout.rs`、`docs/PRODUCT_REQUIREMENTS.md` | candidate identityと既存Release asset集合のlocal resolver/publisher fixture、Linux-only差分のWindows追加選択、product-impact runのLinux authority欠落fixture | implemented |
| LINUX-BUNDLE-CONTENTS-01 | bundle archiveはbyte-identicalなruntime launcher `run.sh`、実行可能payload `codex_info`、`install.sh`、main/update units、license/noticeを含み、version/source/target/mode/size/digestをexternal/internal manifestへ結び付ける | 必須file欠落、非regular、mode違反、異なるidentity混在、manifest byte不一致はinstall/publicationを拒否する | bundle build/test、installer、unit | isolated archive inventory、manifest/member identity fixture | implemented |
| LINUX-BUNDLE-OPERATIONS-01 | 顧客通常導線をbundle install後の`$HOME/.local/bin/codex-info`だけでstart/UI/status/update/stop/disable/removeまで完結させる | repository clone、Cargo build、source/target fallback、raw systemctlを通常操作に要求しない | launcher、installer、README/runbook/wiki | bundle-only installと全launcher commandの有限test | implemented |
| LINUX-BUNDLE-AUTOUPDATE-01 | persistent installerとupdate unitsは元bundle/repositoryなしで使え、boot/startup/manualおよび5分後開始・1時間周期timerが同じfixed-repository resolverを呼ぶ | 次回発見は1時間1秒以内。raw response/秘密をjournalへ記録せず、待ちを無期限にしない | installer、service/timer、launcher、運用文書 | isolated HOME unit inventory、timer値、全trigger同一resolver test | implemented |
| LINUX-BUNDLE-AUTOUPDATE-02 | updaterは最高stable exact-five Releaseを受理し、anti-downgradeを守り、同version/manifest不一致はverified repairする | no candidateはvalid local世代だけ継続。不正Release/redirect/size/digest/identityはinstall state無変更、localも不正ならSAFE_BLOCKED | resolver、installer、security/product spec | stable/equal/older/no-candidate/100-entry/invalid matrix | implemented |
| LINUX-BUNDLE-AUTOUPDATE-03 | candidateのRelease identity、checksum、manifest、archive contentを完全検証してから既存atomic install、`codex-info.service`再起動、source-bound health・recorder identity・`/v1/details state=ready&#124;auth_required`のfunctional readinessへ一度だけ渡し、失敗時は旧binary、旧unit、永続installer、profile dataへrollbackする。install/update/removeは同じuser lockで直列化する | 検証、install、service切替、health、detailsのいずれかの失敗や`initializing&#124;error`はpartial successにせず、途中fileを残さず、既存profile dataを削除しない。競合操作はmutation前に拒否する | Linux update resolver、`packaging/install_linux_bundle.sh`、`packaging/codex-info.service`、`packaging/codex-info-update.service`、`docs/PRODUCT_REQUIREMENTS.md`、`SECURITY.md` | checksum/manifest/archive content tamper、service/health/details failure fixtureのold binary/unit/profile sentinel、single atomic switch/rollback count、concurrent operation rejection | implemented |
| LINUX-BUNDLE-RETENTION-01 | install、update、reinstall、remove、service切替失敗の各経路で導入binary、`$HOME/.local/libexec/codex-info-install.sh`、installed manifest、履歴DB、verified backup、reset hint、session JSONL、設定を保持する | removeは`codex-info.service`と`codex-info-update.service`/`.timer`を停止・無効化しunitだけを解除し、部分導入を成功と表示せず、保持対象を削除しない | `packaging/install_linux_bundle.sh`、`packaging/codex-info.service`、`packaging/codex-info-update.service`、`packaging/codex-info-update.timer`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md`、`README.md`、`wiki/導入と起動ガイド.md` | isolated HOMEのbinary/installer/manifest/profile sentinelとdaemon/update unit lifecycle、rollback retention fixture | implemented |

## Issue #128 — Linux更新収束・managed常駐・記録継続（2026-09-03）

- 目的: installed launcher、service起動前reconcile、timer、手動更新を単一authorityへ収束させ、旧版起動・24時間待ち・daemon不在・記録停止のデッドロックを有限時間で解消する。
- 非目的: Windows updater/sourceの変更、apt/rpm repository、root/system-wide install、別architecture/channel、publisher/signature保証、quota/残量の推測補間、unknown processの停止。
- 不変条件: `desired_state=running`の収束完了はverified new managed+functionally-ready（A）またはverified old managed+functionally-ready（B）だけ。`desired_state`がstopped、disabled、removedの操作は対応する非稼働control stateの完全read-backだけを正常終端とする。foreign/unknown ownerは無変更の`SAFE_BLOCKED`。全waitはbounded、全identityはsource generationへ結合、profile dataは非破壊。
- 依存DAG: exact Release → archive/generation検証 → install journal/current switch → managed activation → source-bound health → recorder state/commit → strict details readiness → gap resolution → launcher/UI consumer。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| U128-01 | live stable Releaseがlocalより新しい場合、launcher/manual/startup/timerの全入口が同じresolverで取得・適用し、source-bound read-backまで完了する | remote metadata、download、switch、activationの部分成功を成功にしない | launcher、installer、units | live/fixture stable convergenceとfull tuple read-back | verified |
| U128-02 | inactive managed serviceとhealthy unmanaged ownerの組合せを成功にしない | known Codex ownerだけを退役させmanagedへ収束。foreign/unknownはsignal 0でSAFE_BLOCKED | launcher、daemon identity | inactive-managed＋unmanaged/foreign listener matrix | verified |
| U128-03 | 引数なし、`--start`、`--ui`、service直接起動がdaemon開始前にbounded reconcileを通る | `--ui`も旧ownerを表示正本として使わず、reconcile失敗時にcandidate UIを実行しない | launcher、service ExecStartPre | 各入口のold/equal/new generation fixture | verified |
| U128-04 | timerはinstall後5分で開始し以後1時間周期、accuracy 1秒で、公開後の次回発見を1時間1秒以内にする | 日次・24時間待ち、無期限pollを禁止 | update timer | unit値と次回時刻境界test | verified |
| U128-05 | candidate選定、検証、download、install、switch、activation、health、recorder、detailsの各失敗で旧世代とprofile dataを保持する | partial file/link/unitや`details state=initializing&#124;error`をcurrent成功として公開しない | resolver、installer | phase別fault injection、details error rejection、sentinel byte/hash | implemented |
| U128-06 | launcher、payload、installer、manifest、3 unit、PID/exe、health、recorder、detailsを同じversion/source/manifest generationへ結合する | version文字列またはhealthだけ一致する混成世代を拒否する | bundle、installer、status/health resolver | 各identity要素の差替えとfunctional readiness rejection | implemented |
| U128-07 | Release resolver/installerを一つだけ持ち、L1 install lock、L2 recorder lock、L3 account writer lockの非循環所有を守る | installerはL2/L3、runtimeはL1を取得しない。競合L1はmutation前に失敗 | installer、daemon/store | lock競合、wait-for graph、caller scan | verified |
| U128-08 | network、local validation、stop、readiness、rollback、operation全体の全waitを固定上限で終了する | timeoutをretry loopやbackground未確定へ変換しない | launcher、installer、daemon | fake clock/process timeout matrix | verified |
| U128-09 | `desired_state=running`のstart/UI/startup/timer/update収束はA=new managed functionally-readyまたはB=old managed functionally-readyだけ、stopped、disabled、removedの操作は要求した非稼働stateの完全read-backだけをcompleted terminalとする | SAFE_BLOCKEDは明示的な非completed安全例外。unmanaged healthy、details error、未確認停止はcompletedでない | transaction/status/control | running A/Bとstopped/disabled/removedのterminal state truth table | implemented |
| U128-10 | absent、coherent old/current、incoherent local、managed active/inactive、known unmanaged、foreign/malformed、active/stale journalを有限prestateとして扱う | 未分類状態を推測成功や削除へfallbackしない | convergence resolver | prestate×trigger有限表 | verified |
| U128-11 | manual、launcher、service startup、timer、bootが同じRelease/installation authorityを呼ぶ | 入口別の候補選択・rollback実装を持たない | launcher、installer、units | 全入口caller/trace identity test | verified |
| U128-12 | status/成功判定はcurrent symlink、manifest、全artifact、systemd MainPID、proc identity、lock、socket FD、health前後、recorder state、strict detailsを再読込する | PID/listener/health/version単独と`details state=initializing&#124;error`の成功を拒否 | launcher、server/daemon | tuple各要素のTOCTOU・mismatchとdetails error rejection test | implemented |
| U128-13 | 旧version healthが200でもcurrent世代のmanaged owner確認前に成功しない | old healthをstartup/updateの早期成功へ流用しない | launcher、convergence test | old healthy→new delayed/fail regression | verified |
| U128-14 | 同一profileでsystemd managed recorderをexactly 1つだけ許可する | update/UI/direct startで2つ目のwriter/listenerを作らない | daemon、service、launcher | concurrent activation/lease E2E | verified |
| U128-15 | recorderはowner identity、write state、partition hash、data generation、collector epoch、cycle、last commitをowner-only stateへcommit後だけatomic記録する | heartbeatだけでDB commitを捏造しない | daemon、usage store | recorder-state exact schema/mode/order test | verified |
| U128-16 | worker死亡を2秒以内に検知し、2回目/fatalはnonzero exitして`Restart=always`へ渡し、systemdが5秒間隔かつstart limitなしで再起動を継続する | 同callback retry、busy loop、StartLimitによる永久inactive、反復失敗の成功表示を禁止 | daemon、service | worker fault、`RestartSec=5s`、`StartLimitIntervalSec=0` unit contract | implemented |
| U128-17 | 操作失敗を返す前にverified old listener/recorder/healthと次回update triggerを復旧するかSAFE_BLOCKEDへ入る | old世代をusableだったunmanaged状態へ戻さない | installer、launcher | phase別rollback terminal oracle | verified |
| U128-18 | 停止中のSession usageだけをverified cursor/rangeからbackfillし、quota/残量は補間・複製しない | collector全停止区間の未観測quotaを0・last value・線形値で生成しない | daemon、usage store | outage/backfill/zero-new-row fixtures | verified |
| U128-19 | gap ledgerはexact schemaとpending/recovered/confirmed/rejected遷移を持ち、confirmedだけを既存`history_gaps`へ公開する | timestamp差だけでgapを作らず、再実行で重複しない | usage store、server | gap transition/idempotence/projection tests | verified |
| U128-20 | stable Release選択とinstallはsemantic-version anti-downgradeを守る | localより古いRelease、tag/version不一致をcurrentへしない | resolver | newer/equal/older/malformed SemVer table | verified |
| U128-21 | control-stateはdesired state、publicationはverified generation/ownerを別authorityとして持つ | control応答・markerだけでruntime/health成功を表明しない | launcher、daemon/server | desired×actual state matrix | verified |
| U128-22 | installed/repository launcherはCargo buildを行わない | Cargo、`target/`、source checkout不在でも起動操作が完結する | `run.sh`、bundle | fake Cargo拒否・bundle-only E2E | verified |
| U128-23 | repository `run.sh`とarchive member `run.sh`をbyte-identicalにする | build時生成の別launcherや埋込み修正を拒否 | bundle builder/test | SHA-256 byte comparison | verified |
| U128-24 | manifestは`run.sh`を含む全regular memberのexact path/mode/size/SHA-256を保持する | 欠落・余分・duplicate・mode/size/hash違いをinstall前に拒否 | bundle/installer | member単位tamper matrix | verified |
| U128-25 | install journalとcurrent switchはatomic write、file fsync、parent directory fsyncを各phaseで行う | crash後はjournal identityだけでresume/rollbackし、mtime推測を使わない | installer | phaseごとのcrash image recovery | verified |
| U128-26 | generation/current、`$HOME/.local/bin/codex-info` launcher、`$HOME/.local/bin/codex_info` payload、persistent installer、manifest、3 unit、control/transaction/recorder stateのpathとmodeを固定する | symlink escape、非regular、owner/mode違反、任意pathを拒否 | installer、launcher、daemon | path/mode/symlink matrix | verified |
| U128-27 | repository copy、Cargo build、`target/`、別binary、古いunverified artifactへfallbackしない | installed generationが不正ならremote verified repairまたはSAFE_BLOCKED | launcher、installer | fallback canary/path scan | verified |
| U128-28 | installed launcherはno args/`--start`、`--ui`、`--stop`、`--disable-autostart`、`--remove`、`--status`、`--update`、`--help`だけを提供する | unknown/mixed/`--port`はmutation 0で非0終了 | `run.sh`、`src/i18n.rs`、`src/main.rs`、`scripts/test_run_launcher_version_sync.sh`、`scripts/cli_contract_e2e.sh` | accept/reject argv matrix | verified |
| U128-29 | `--remove`はmain/update unitだけを解除し、generation/launcher/installer/manifest/profile dataを保持する | DB、backup、hint、gap/recorder/control state、Session、settingsを削除しない | launcher、installer | sentinel retention E2E | verified |
| U128-30 | 新launcherから旧payload、旧launcher/installerから新payloadを含むreverse compatibilityと全prestateを有限fixtureで検査する | 変更不能な旧updaterのmanaged正常/listenerなしからのsafe bootstrapと、旧unmanaged障害時にnew installerを直接使うjournal付き回復を分ける。一方向happy pathやjournalなしsignalで完了しない | convergence test | reverse launcher/version matrixとpredecessor managed/unmanaged recovery | verified |
| U128-31 | Release API単一100件応答、最高stable exact tag、exact 5 asset、canonical URL、size/digest、最大3 redirectを検証する | 100件、duplicate/extra/missing、draft/prerelease、redirect逸脱を拒否 | resolver | HTTP fixture全reject/accept表 | verified |
| U128-32 | Windows source/updater/fallbackは変更せず、同Release version/asset identityだけをlive oracleで確認する | Linux local testをWindows実装変更・実Windows動作PASSへ読み替えない | Linux-only scope、live read-back | git diff path scan＋公開Release asset read-back | verified |
| U128-33 | update失敗時の回復attemptは各phase1回で、復旧後のUIはexact旧source identityだけを受理する | 別旧版、versionだけ同じartifact、無限retryを許可しない | installer、launcher/UI launch | rollback identity/attempt counter test | verified |
| U128-34 | system-wide/root package、apt/rpm、他arch/distro保証、署名/publisher保証、quota復元は非対象とする | 未指定authorityを推測・表明・実装しない | `DESIGN.md`、`SECURITY.md`、`README.md`、`README.en.md`、`docs/PRODUCT_REQUIREMENTS.md`、`docs/REQUIREMENTS_LEDGER.md`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md`、`docs/DATA_PROTECTION_POLICY.md`、`docs/LIVE_STATE_DECISION_MATRIX.md`、`docs/REST_API_V1.md`、`wiki/Home.md`、`wiki/導入と起動ガイド.md`、`wiki/開発・運用メモ.md`、installer | diff/manifest/customer copy scan | verified |
| U128-35 | Release30秒、asset各300秒、update20分、control30秒、local validate/publish60秒、stop20秒、readiness30秒、rollback60秒、manual/startup20分30秒を上限にする | 数値欠落、0、overflow、無期限waitを拒否 | launcher、installer | exact constant/timeout boundary tests | verified |
| U128-36 | generation名を`<version>-<source_sha>-<manifest_sha256>`、control/install journal/recorder stateをversioned exact schemaとして固定する | unknown/missing/duplicate field、identity不一致をmutation前に拒否 | `run.sh`、`packaging/install_linux_bundle.sh`、`packaging/codex-info.service`、`packaging/codex-info-update.service`、`packaging/codex-info-update.timer`、`scripts/build_linux_bundle.sh`、`scripts/test_linux_bundle.sh`、`scripts/test_run_launcher_version_sync.sh`、`scripts/install_systemd_recorder.sh`、`scripts/record_daemon_e2e.sh`、`scripts/cli_contract_e2e.sh`、`scripts/test_linux_update_convergence.sh`、`src/main.rs`、`src/daemon.rs`、`src/usage_store.rs`、`src/server.rs`、`src/account_scope.rs`、`src/security.rs`、`tests/db_protection_runtime.rs`、`tests/release_linux_rollout.rs` | schema/domain/unknown-key matrix | verified |
| U128-37 | newer Releaseなし・同versionでもlocal coherenceを全要素検査し、不整合なら同Releaseからverified repairする | version equalityだけの早期no-opを禁止 | resolver、installer | equal-version coherent/no-op と各1要素corrupt/repair | verified |
| U128-38 | remote candidate不在時は完全なlocal世代をBとしてmanagedかつfunctionally readyへ戻し、localも不正またはdetails errorならSAFE_BLOCKEDにする | 不正local起動、unmanaged成功、healthだけの成功、無根拠download fallbackを禁止 | launcher、installer | no-candidate×valid/invalid localとdetails state fixture | implemented |
