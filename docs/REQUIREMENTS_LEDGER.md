# 要求管理台帳

この台帳にない振る舞いは、実装・評価・PRの対象にしない。要求を追加・変更した場合は、先にここへ安定ID、境界、失敗動作、独立オラクルを登録する。`verified`以外の行を残したまま最終ゲートを通過させてはならない。

## Windows版をX版品質へ揃える（2026-08-27）

- 目的: Windows版のデータ意味論、状態遷移、失敗時保持、Graph、Threads、起動表示、操作品質を、X版の検証済み正本と同等にする。
- 非目的: X版のレイアウトを無条件に複製すること、未取得値の推測、実Windows未確認項目のPASS化、PR・Releaseを要求完成前に開始すること。
- 不変条件: 同一fixtureの値・期間・欠測・残量・thread意味論はplatform間で一致する。部分世代、stale completion、失敗応答はlast-good rootを壊さない。Windows固有コードは表示・入力adapterに限定し、第二のデータ正本を作らない。
- 依存DAG: 共通wire/fixture正本 → Windows validation/state generation → Main/Graph/Threads projection → Window/Input/Accessibility adapter → 実Windows連結評価 → 最終判定。
- raw clause対応: 「Windows側の品質をX版にまで」→ WIN-PARITY-DATA/STATE/UX/OPS、「計画→分析→実装設計→TDD→連結評価→結果→判断→終了」→ PROC-WIN-PHASE-01、「Luna-maxをユニット単位、SOLは計画・設計・最終評価」→ PROC-WIN-DELEGATE-01、「`.com/{UUID}_req.md`/`_res.md`」→ PROC-WIN-HANDOFF-01、「1分周期、待機中は状態通知以外を読まない」→ PROC-WIN-MONITOR-01、「未完なら再計画して反復」→ PROC-WIN-CLOSURE-01。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| WIN-PARITY-DATA | Windows版はX版と同じquota、model usage、履歴期間、欠測、未使用帯、残量独立性、thread live意味論を同一fixtureから表示する | 重複時刻、初回未観測、長時間gap、reset境界、全model 0/複数、thread 0/複数/不正を推測で補わない | `src/*`、`tests/fixtures/*`共通wire/fixture、`windows-client/src/CodexInfo.WindowsClient.Core/*`、`windows-client/src/CodexInfo.WindowsClient/Graphing/*`、`windows-client/src/CodexInfo.WindowsClient/ViewModels/*` | X/Windowsが別helperではなく同じ固定fixture期待値を通るunit、REST fixture、実Windows Graph/Threads画像 | verified |
| WIN-PARITY-WIRE-01 | `/v1/details`は`history_gaps`を含む正本のexact 13 top-level keyだけを受理し、欠落・未知・重複keyまたはcontract revision不一致を全体拒否する | 12-key互換fallback、未知key無視、部分採用、last-good上書きを禁止する | `src/server.rs`、Windows Core REST parser/domain | exact valid fixtureと各1-key欠落・追加・重複fixture、旧root保持unit | verified |
| WIN-PARITY-HISTORY-01 | history sampleはminute-startかつraw `(reset_at,timestamp)`一意で、exactly one canonical period範囲内に属し、canonicalize後の`(period.id,timestamp)`も一意な行だけから候補を構成する | raw/canonical duplicate、非minute、period外、順序上書き、coalesce/max/last-row/null化を候補全体rejectとし部分採用しない | 共通REST fixture、Rust admission、Windows Core admission | raw duplicate、moving-reset canonical collision、各failureを単独注入した固定fixtureと全体reject/last-good不変検査 | verified |
| WIN-PARITY-CURRENT-01 | current periodの`end_at`は観測時刻までの有効端`min(reset_at, observed_at)`、`reset_at`はsample所属用canonical境界として別に保持する | 将来resetまで横軸を伸ばす、`end_at`をreset境界へ流用する、過去期間の確定境界を書換えることを禁止する | Rust period projection、Windows `EffectiveGraphEnd` | X/Windows同一fixtureのcurrent/過去右端exact値 | verified |
| WIN-PARITY-FIRSTOBS-01 | 初回未観測区間はX版と同じ水平baselineを表示し、最初の実観測時刻に垂直なfirst-observation境界を置く | 未観測区間を斜線の利用量へ補間せず、idle-bandの可視色・高さ・境界を別系列の値から推測しない | 共通graph fixture、Rust/Windows projectionとrender adapter | 独立固定期待値、no-diagonal pixel/geometry検査、同revision実Windows Graph画像 | verified |
| WIN-PARITY-STATE | Windows版は初回完全世代まで固定スピナーを表示し、定期更新・再接続・部分失敗・stale completionで画面を空にせず、完全なlast-good rootを保持する | health/status/details不一致、timeout、cancel、順序逆転、threadだけ失敗、初回失敗、再試行を有限state tableで固定 | `windows-client/src/CodexInfo.WindowsClient/ViewModels/*`、`Infrastructure/*`、`MainWindow.axaml` | state-machine unit、API integration、実Windows startup/refresh/failure UIAと動画または連続capture | verified |
| WIN-PARITY-PAIR-01 | `/v1/status`と`/v1/details`が同じexact `Codex-Info-Published-Pair: v1:<128-bit epoch hex><128-bit publish-counter hex>`を持ち、schema/domain/common coreも一致する候補だけをWindowsが一つのroot notificationとしてcommitする | production listenerはdefault initializing pairをcounter=1でaccept前に構築しpairなし200を0件とする。header欠落・重複・形式不正・不一致・旧世代completion・部分応答ではlast-goodを保持し、同cycle自動retryを0回。counterは成功publishだけ増やし、CSPRNG/初期pair失敗はlisten前非0終了、overflowは旧pair不変でservice停止 | `docs/REST_API_V1.md`の`rest-v1-published-pair-header-20260827` authority、Rust SnapshotPublisher/response、Windows parser/state root | 注入epoch固定vector、起動直後counter1、最初のdata publish counter2、同epoch連続publish、別epoch、reject/read不変、順序逆転・cancel・不一致のatomic commit unit | verified |
| WIN-PARITY-RETRY-01 | saved selectorは次回起動時だけ再接続に使い、同一generationの自動retryは0回、利用者の明示Retryは新generationを一つだけ開始する | 旧child callback、重複click、cancel後completion、supervisor child exitで同世代を再利用しない | Windows connection supervisor、Main state owner | generation ID、child起動/reap回数、stale callback廃棄を数えるfinite state test | verified |
| WIN-PARITY-UX | Windows版のMain/Setup/Settings/Graph/Threads/Legalは正本geometry、DPI、配置、drag、keyboard、focus、accessibility、single-version表示を満たす | 900×480 fixed群、Graph 700×480以上、複数monitor、text scale、high contrast、child singleton、見えない配置をPASSにしない | `windows-client/src/CodexInfo.WindowsClient/*.axaml*`、window/input/accessibility adapter、Presentation tests、E2E | UIA geometry/pixel/keyboard checks、全登録surfaceのfresh Windows capture、独立意味棚卸し | verified |
| WIN-PARITY-CTA-01 | failure surfaceのprimary CTAは状態ごとに1個とし、auth-start完了後にだけauth-checkへ置換する | auth-start/auth-check/retry/updateを同時primary表示せず、失敗状態とCTA対応を有限表外へfallbackしない | Windows Main/Setup view-modelとAXAML | state×CTA cardinality table、AutomationId可視数exact検査、fresh Windows capture | verified |
| WIN-PARITY-LEGAL-01 | Legalは固定viewport内のchapter/page表示とし、chapter position、Back、Close、Minimizeを常時操作可能にする。埋込みMarkdown正本は見出し・箇条書き・リンク・強調・inline code・code fence・HTML commentの記法を表示用通常テキストへ変換し、法的内容とURLを欠落させない | whole-text/root ScrollViewer、viewport外control、本文欠落、Markdown記号のraw表示、`.txt`/拡張子なしライセンス本文の改変を禁止する。変換不能・resource欠落時は既存fail-closed pageを表示する | Windows legal catalog/plain-text projection/window/view-model、Presentation tests、E2E | 固定Markdown→plain oracle、全埋込み`.md`の禁止記法scan、全page到達unit、900×480 UIA/text/capture意味棚卸し | verified |
| WIN-PARITY-OPS | Windows版は接続、Setup/Settings、SSH/WSL起動、再接続、更新通知、install/update/uninstallで既存設定・履歴・last-goodを破壊しない | 初回/再起動/保存失敗/取消/接続不能/更新失敗/再入/重複操作を有限transactionとして扱う | `windows-client/src/*`、installer/update tools、Windows tests/E2E | process argv test、settings persistence、installer/update transaction tests、実Windows導入/復旧証拠 | verified |
| WIN-PARITY-RECOVERY-01 | raw SSH host/userはsaved selector復旧用の明示one-session経路でだけ受け取り、通常Setupには表示せず永続化件数を0にする | settings、argv、stderr、log、履歴へraw/展開済み値を残さず、session終了後に再利用しない | Windows Setup/connection/settings/process adapter | persistence/file/log scan、process argv test、再起動後非再利用test | verified |
| WIN-PARITY-EVIDENCE-01 | WSL deterministic testと実Windows連結評価を分離し、同じproduct revisionの全surface・全failure state証拠が揃うまで最終PASSにしない | test 0件、SKIP、未取得画像、source/image revision不一致、capture取得だけをPASSへ昇格しない | `windows-client/tests/*`、`windows-client/tools/*`、artifact provenance、semantic image review | non-zero test count、binary/source identity、UIA/pixel/意味棚卸しを結合したfresh host evidence | verified |
| PROC-WIN-PHASE-01 | 作業は計画→分析→実装設計→TDD実装→連結評価→結果報告→判断→終了の順に進める | 要求・設計・対象unitが未確定の状態で実装または最終ゲートを開始しない | `.com/*_req.md`、`.com/*_res.md`、`docs/REQUIREMENTS_LEDGER.md` | phaseごとの成果物と後続開始条件の照合 | verified |
| PROC-WIN-DELEGATE-01 | SOL親が計画・設計・統合・最終評価を所有し、Luna-max子が狭い非重複unitの分析・TDD実装・連結評価を行う | 子は指示外編集・他差分の巻戻し・未実行PASSを行わない | `.com/*_req.md`、`.com/*_res.md` | req ownershipと実diff、res command evidenceの対応 | verified |
| PROC-WIN-HANDOFF-01 | 各Luna unitは`.com/{UUID}_req.md`を入力正本とし、完了時に対応する`{UUID}_res.md`へ結果を書く | res欠落、UUID不一致、指示外成果は回収済みにしない | `.com/*_req.md`、`.com/*_res.md` | UUID一対一・必須section検査 | verified |
| PROC-WIN-MONITOR-01 | SOL親はLuna作業中、1分間隔でagent status通知だけを待ち、待機中にrepo・ログ・差分を読まない | 1分未満のpoll、完了前のres存在確認、重複回収、同一入力の再実行をしない | `.com/*_res.md`、agent status | 1分wait履歴と一度だけのres回収 | verified |
| PROC-WIN-CLOSURE-01 | 全res回収後にSOLが条項別証拠を判断し、未完なら原因単位で再計画して新UUIDへ反復する | FAIL/INCONCLUSIVE、実Windows未確認、変更後の古い画像を終了扱いしない | `docs/REQUIREMENTS_LEDGER.md`、`.com/*_res.md` | 全行verified、fresh独立評価、同一revision実Windows証拠 | verified |

## 起動CLI再設計（2026-08-27）

- 目的: 利用者向け起動契約を、無印、`--ui`、`--port PORT`、`--stop`、`--help`だけへ限定する。
- 非目的: LAN公開、任意bind address、内部収集テスト用CLI、旧オプション互換を提供しない。
- 不変条件: 待受addressは常に`127.0.0.1`、同一profileのdaemon ownerは1つ、停止対象はlock identityで検証したPIDだけ、未知・重複・欠落・範囲外引数は副作用なしで拒否する。
- 依存DAG: CLI parse → loopback config → service/UI lifecycle → Windows起動引数・systemd/E2E → 多言語help。
- raw clause対応: 「無印も引数ありもloopback」→ PROC-LAUNCH-01、「常駐解除」→ PROC-STOP-01、「不要な`--service`/`--ui-only`/`--all`/`--record-daemon`」→ PROC-OPTIONS-01、「各オプションの動作テスト」→ PROC-OPTIONS-01/PROC-STOP-01。

### 有限受理・拒否表

| argv | 終了/動作 | 副作用契約 |
| --- | --- | --- |
| なし | daemon+REST、127.0.0.1:8787 | Windowなし、owner最大1 |
| `--port P`、P=1..65535 | daemon+REST、127.0.0.1:P | Windowなし、非loopback bindなし |
| `--ui` | daemon+REST確保後にX UI | healthy owner再利用、無ければ1つだけ開始 |
| `--ui --port P`、P=1..65535 | 127.0.0.1:Pを使う上記UI | argv順序はこの形だけ |
| `--stop` | 下記停止契約 | daemon/REST/UIを新規生成しない |
| `--help` / `--h` / `-h` | locale別help、終了0 | daemon/REST/UI/DB変更なし |
| 旧6 option、未知語、誤記、重複、欠落、余分な位置引数、逆順、`--port=P`、help/stopとの混在、P=0/65536/負数/非数値/Unicode不正 | 終了非0 | daemon/REST/UI/DB/lock変更なし |

### `--stop`停止契約

- profileのlock pathが存在しなければ停止済みとして終了0。
- lock pathが存在するが、完全なlock identity・PID starttime・executable identityを検証できない場合は、何もsignalせず終了非0。
- 検証済み所有PIDにTERMを1回だけ送り、最大5秒間、同じlock pathが消えることを待つ。SIGKILLへ昇格しない。
- signal失敗、5秒timeout、停止中に別ownerが同じlockを取得、lockが残る場合は終了非0。
- DB、backup、reset hint、source JSONLの内容と存在を変更しない。listenerだけが存在しlock ownerを証明できない場合は無関係processとしてsignalしない。

| ID | 要求（観測可能な契約） | 境界・失敗動作 | 実装範囲 | 独立オラクル | 状態 |
| --- | --- | --- | --- | --- | --- |
| X-START-01 | X版の初回認証済み遷移では、ヘッダーと製品バージョンを固定し、quota/local usage/threadの部分世代を段階表示しない | 未認証画面は認証操作を表示し、認証済みだが未完成の間は内容領域を公開しない | `ui/app.slint`、`src/main.rs`、`src/main.rs::sync_ui`、`scripts/x11_startup_visual_gate.sh` | native startup state test、X11 fresh startup visual gate | verified |
| X-START-02 | X版は最初の完全な利用世代が揃うまで内容領域にスピナーを表示する | quota取得済みでもlocal usage未完了なら継続。完全世代公開後に解除 | `ui/app.slint` の `startup-loading`、`scripts/x11_startup_visual_gate.sh` | `native_startup_loading_requires_a_complete_authenticated_generation`、X11 startup visual gate | verified |
| X-START-03 | X版の初回local収集/app-server失敗時はスピナーを解除し、最後の完全表示または失敗状態を表示する | 部分値・0・未取得へ黙って置換しない | `src/main.rs::native_startup_loading`、status state | `native_startup_failure_releases_loading_surface` | verified |
| X-START-04 | X版の起動ウィンドウは主モニターの可視デスクトップ内へ配置し、利用者から見えない位置へ出さない | 別モニター・負座標・デスクトップ外の場合は合格にしない | `src/main.rs`、`run.sh`、`scripts/x11_startup_visual_gate.sh` | visible position test、X11可視範囲ゲート、実画面キャプチャ | verified |
| X-START-05 | `--ui` のdaemon/REST起動失敗時もX版GUIを表示し、接続失敗と再試行手段を提示する | サービス失敗でプロセス・ウィンドウを即時終了させない | `src/main.rs`、`run.sh` | 失敗ポートでの実起動保持確認、失敗状態テスト | verified |
| X-GRAPH-01 | X版の過去グラフは期間境界・累積モデル値・未使用帯・残量を正本どおり表示する | 未観測長時間を斜めの使用量へ変換しない。残量をモデル使用量から推測しない | `src/main.rs`、`src/main.rs` graph projection | fixed graph fixture tests、X11 graph visual gate | verified |
| X-GRAPH-02 | X版をquota reset後に新規起動してGraphを開いた場合、minute-start保存された境界bucketを含む現在期間を自動選択し、selector・期間ラベル・sample・全描画系列を同じcanonical resetへ揃える | authoritative開始境界より1分以上早いowned rowを含む現在期間は全体を拒否し、旧期間へ混在させない。reset aliasのauthority許容幅は変更しない | `src/main.rs::apply_authoritative_current_bounds`、fresh startup graph selection/projection | boundary bucketと1分前の独立固定期待値、全graph unit、同一release revisionのfresh X実画面と周期refresh後再確認 | verified |
| X-THREAD-01 | X版thread取得が不完全でも、完全な前回表示を部分値で上書きしない | malformed token_count envelopeだけは非livenessデータとして読み飛ばし、lifecycle/model不正は全体失敗 | `src/thread_contract.rs`、`src/main.rs` | parser/failure-class exact tests | verified |
| WIN-START-01 | Windows版の初回起動はhealth・status・detailsの一致した完全世代まで内容領域を隠し、固定位置のスピナーを表示する | 部分世代の順番描画・画面全体のばたつきを許可せず、失敗時は再試行を表示 | `windows-client/src/CodexInfo.WindowsClient/MainWindow.axaml`、`windows-client/src/CodexInfo.WindowsClient/ViewModels/MainWindowViewModel.cs`、`windows-client/tests/CodexInfo.WindowsClient.Presentation.Tests/MainWindowViewModelTests.cs` | Windows presentation tests、実Windows UI Automation/E2E | verified |
| WIN-GRAPH-01 | Windows版の過去グラフはX版と同じ期間境界・累積値・未使用帯・残量独立性を表示する | 未観測区間を使用量へ推測せず、専用ニュートラル色を失わない | `windows-client/src/CodexInfo.WindowsClient/Graphing`、`windows-client/tools/Run-WindowsClientE2E.ps1` | Windows graph projection tests、実Windows過去期間画像 | verified |
| WIN-VERSION-01 | Windows版の製品バージョンはメイン画面に一度だけ表示し、子ウインドウのタイトルへ重複表示しない | X版と同じversion authorityを使用し、表示欠落・重複を許可しない | `Cargo.toml`、`Cargo.lock`、`windows-client/Directory.Build.props`、`windows-client/src/CodexInfo.WindowsClient/MainWindow.axaml`、`windows-client/src/CodexInfo.WindowsClient/ViewModels/MainWindowViewModel.cs` | Windows contract gate、実Windows UI Automation | verified |
| PROC-LAUNCH-01 | 起動モードを固定し、引数なし/`--port PORT`はloopback daemon+RESTのみ、`--ui`はdaemon+REST+X UI、`--ui --port PORT`は指定portで同じ動作をする | portは1..65535、addressは127.0.0.1固定。モード不一致、非loopback bind、二重daemon所有を合格にしない | `src/main.rs`、`run.sh`、`packaging/codex-info.service`、`README.md`、`README.en.md`、`DESIGN.md`、`SECURITY.md`、`scripts/record_daemon_e2e.sh`、`scripts/cli_contract_e2e.sh`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md`、`docs/REST_API_V1.md`、`docs/DATA_PROTECTION_POLICY.md`、`docs/WINDOWS_CLIENT.md` | 全受理形・port境界unit test、モード別実行、REST health | verified |
| PROC-STOP-01 | `--stop`は同一profileのlock identityで検証した常駐daemonだけへTERMを送り、lock解放まで待つ。停止済みなら成功する | 無関係PIDを停止しない。timeout、signal失敗は非0。DB/historyを削除しない | `Cargo.toml`、`Cargo.lock`、`src/main.rs`、`src/daemon.rs`、`scripts/cli_contract_e2e.sh`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md` | isolated HOMEでstart→health→stop→lock/listener消失、停止済み冪等test | verified |
| PROC-OPTIONS-01 | 公開引数は無印、`--ui`、`--port PORT`、`--ui --port PORT`、`--stop`、`--help`/`--h`/`-h`だけとする | `--service`、`--ui-only`、`--all`、`--listen`、`--record-daemon`、`--once`、誤記、重複、欠落を副作用なしで拒否する | `src/main.rs`、`src/i18n.rs`、`run.sh`、`scripts/record_daemon_e2e.sh`、`scripts/cli_contract_e2e.sh`、`docs/*`、`windows-client/src/*`、`windows-client/tests/*` | finite accept/reject matrix、旧option repository scan、helpとparser集合一致 | verified |
| PROC-HELP-01 | `--help`/`--h`/`-h`は起動せず、既存のi18nカタログで利用可能な起動モードと動作を表示する | ヘルプ要求でdaemon・REST・GUIを生成しない、固定単一言語の製品メッセージを出さない | `src/i18n.rs`、`src/main.rs`、`run.sh`、`docs/CUSTOMER_OPERATIONS_RUNBOOK.md` | 全対応言語のcatalog test、help mode unit test、`LC_ALL`を切り替えた`run.sh --help`実行ログ | verified |
| PROC-I18N-01 | 起動スクリプトを含む利用者向け固定メッセージは、本体と同じ対応localeのi18nカタログから導出する | 単一言語の製品文言をスクリプトへ複製しない | `src/i18n.rs`、`src/main.rs`、`run.sh`、`docs/PRODUCT_REQUIREMENTS.md` | 全対応言語の`launch_help`テスト、script固定文言検査、`LC_ALL`切替実行 | verified |
| PROC-LEDGER-01 | 要求台帳にない要求を実装・評価・PRに含めず、local入口はschemaと実装状態、完了時の`--final`はremoteを含むverified状態を確認する | PRでしか得られない証拠をPR前に要求せず、台帳行追加後は旧証拠を無効化する | `run.sh`、`.github/workflows/rust.yml`、`.github/workflows/windows-client.yml`、`.github/workflows/release.yml`、`docs/PRODUCT_REQUIREMENTS.md`、`docs/REGRESSION_PREVENTION_POLICY.md`、`scripts/regression_guard.sh`、`scripts/windows_client_contract_gate.sh`、`scripts/x11_graph_visual_gate.sh`、`scripts/requirements_ledger_gate.sh`、`docs/REQUIREMENTS_LEDGER.md` | local schema gate、remote証拠取得後のfinal gate | implemented |
| WF-BINARY-IMPACT-01 | `main`向けsame-repository PRの完全Git差分をbranch名に依存せず有限ownerへ分類し、binary impactとCodeQL言語を同じ選択から導出する | callerがrename/copy検出を明示して両端とdeleteを渡し、空差分・未知pathは失敗する。各event内でclassifierは1回だけ使用する | `AGENTS.md`、`scripts/ci_change_scope.py`、`scripts/test_ci_change_scope.py`、`.github/workflows/*` | tracked全path、実Git copy、add/delete/rename/unknown/空差分、actionlint | implemented |
| WF-FEAT-SELECTIVE-01 | `feat/next`向けPRは任意same-repository headの完全差分を分類し、選択ownerだけを評価して通常job `feat-acceptance`を成功させる | selected異常、non-selected実行、分類不能を失敗にし、version・candidate・Release mutationは0件とする | `.github/workflows/feat-integration.yml`、`.github/workflows/selective-quality.yml`、分類・結果gate | owner全31組合せ、異常result、実PR job check | implemented |
| WF-QUALITY-ONCE-01 | 各eventで分類器を最大1回使用し、H0 runが生成したH1を含むfinal headでselected ownerとCodeQL言語だけを各1回実行する | 結果oracleはtrusted base版を使う。生成H1 observerは先行H0 identityがある場合だけowner 0回、identityのない手動commitとH2は評価する。selected異常とnon-selected実行を失敗にする | `.github/workflows/*`、`scripts/selected_quality_gate.py`、`scripts/test_selected_quality_gate.py`、`scripts/workflow_quality_gate.py`、`scripts/test_codeql_workflow.py`、`scripts/windows_client_contract_gate.sh` | owner全31組合せ、PR側oracle改変拒否、各異常result、H0→H1 observer→H2、手動version | implemented |
| WF-MERGE-GATE-01 | trusted `pull_request_target`の通常job `version-prepared`と`acceptance`をrequired checkにし、token生成H1だけ同じrunの最終結果2件をH1へ作る | H1 checkはproducer run IDを持つ。H1 observer判定のidentity取得は1回だけとし、汎用reporter、poll、retry、mutation readback、URL照合、branch allowlistを追加しない | `.github/workflows/version-prepare.yml`、`.github/workflows/feat-integration.yml` | actionlint、local H0/H1 observer/check状態遷移、実PR check | implemented |
| WF-POSTMERGE-01 | merge後は生成H1 observerなら外部IDのH0 producer、それ以外なら最新event-head runを取得し、Windows jobがskippedならmutation 0、successなら同runのcandidateだけを公開する | observerをproducerにしない。最新非observer run/job/candidateのmissing・failure・cancelを拒否し、旧成功fallback、PR再分類、verdict artifact、品質再実行を行わない | `.github/workflows/release.yml`、`.github/workflows/windows-client.yml` | closed未merge、Windows skip/success、observer、後続failure/cancel、candidate欠落、実artifact transfer | implemented |
| WF-OVERQUALITY-REMOVE-01 | 汎用custom-check reporter、poll/readback、証拠専用artifact、二段acceptanceをworkflow authorityから除去する | `scripts/feat_integration_check_reporter.py`、`scripts/test_feat_integration_check_reporter.py`、`scripts/final_head_check_reporter.py`、`scripts/test_final_head_check_reporter.py`、`scripts/final_acceptance_gate.sh`、`scripts/final_acceptance_gate_test.sh`、`scripts/release_candidate_gate.sh`、`scripts/release_candidate_gate_test.sh`、`scripts/release_quality_run_resolver.py`、`scripts/test_release_quality_run_resolver.py`、`scripts/release_state_gate.py`、`scripts/ci_trust_fixture.py`を削除し、同等fallbackを追加しない | workflow、上記旧script、`docs/PRODUCT_REQUIREMENTS.md`、`docs/REGRESSION_PREVENTION_POLICY.md`、`docs/REQUIREMENTS_LEDGER.md` | repository実行参照0件、actionlint、local workflow gate | implemented |
| VER-AUTO-PATCH-01 | バイナリ影響ありPRだけ、品質確認前に共有versionのpatchを十進整数でちょうど1増やす。バイナリ影響なしPRはversion 3ファイルを変更しない | patchからminorへcarryしない。期待元version不一致、malformed、同期不一致、分類処理失敗では3ファイル無変更で停止する | `.github/workflows/version-prepare.yml`、`.github/workflows/windows-client.yml`、`scripts/ci_change_scope.py`、`scripts/product_version.py`、`Cargo.toml`、root `Cargo.lock`、`windows-client/Directory.Build.props` | patch境界fixture、PR #30 no-binary-impact、version mutation 0、byte不変検査 | implemented |
| VER-SERIES-FIXED-01 | 自動採番はmajor/minorを変更せず、両値は利用者の明示指示を伴う別変更でだけ更新する | 通常自動処理のcarry、推測、既定値置換を禁止する | `.github/workflows/version-prepare.yml`、`.github/workflows/windows-client.yml`、`scripts/product_version.py`、`scripts/test_product_version.py` | major/minor不変fixture、通常PRの無断series変更fail-closed検査 | verified |
| WF-SERIAL-01 | version更新はPR単位、Releaseはrepository単位で直列化する | concurrent head更新はnon-force pushを失敗させ、retry・上書きしない。Releaseは最新runだけを採用し、途中失敗は非公開Draftを残す | `.github/workflows/version-prepare.yml`、`.github/workflows/release.yml` | stale head、push拒否、後続run失敗、draft失敗 | implemented |
| SEC-CI-TRUST-01 | contents/checks write jobはtrusted baseまたはtrusted run outputだけを実行し、PR headをGit object dataとして読む。Release jobはsourceをcheckoutしない | fork、不正version、競合push、producer/candidate欠落ではmutationを開始または継続しない | version・release workflow、local trust gate | actionlint、untrusted checkout検査、non-fast-forward fixture、check mutation fixture | implemented |
| SEC-CODEQL-LOG-01 | protocol検証失敗を診断してもtoken、authorization、password、private key、raw endpointをcleartext logへ渡さない | failure時も固定非秘密markerだけを出し、秘密値の補間・Debug表示・長さによる復元可能情報を禁止 | `src/protocol_contract.rs`のCodeQL指摘経路 | alerts #2-#5 dataflow、対象unit、CodeQL再解析 | verified |
| SEC-MERGE-CODEQL-01 | owner選択から導出したCodeQL言語だけをH1でexact 1回実行し、`acceptance`・`version-prepared`とanalysis成功、errorおよびcritical/high finding不在を必須とする | selected言語のpending/missing/failure、余分な言語実行、alert dismiss、古いheadではmerge不可。Windows client/acceptanceでCodeQLを再実行せず、workflow内でlive rulesを再監査しない | `.github/workflows/codeql.yml`、`.github/workflows/selective-quality.yml`、`scripts/test_codeql_workflow.py`、`scripts/selected_quality_gate.py`、`scripts/workflow_quality_gate.py` | owner→actions/csharp/python/rust全写像、selected/nonselected結果mutation、PR #67/#68相当、実CodeQL check | implemented |
| SEC-CODEX-REVIEW-01 | 変更が確定したPR最新headに`@codex review`を1回だけ投稿し、同headのCodex reviewを補助レビューとして使用する | 新commit後の旧review、non-outdatedかつ未解決のP0/P1、review未完了ではready/releaseをHOLDする。独自API key workflowやrequired statusを追加せず、CodeQL・acceptance・必要な承認を代替しない | Codex GitHub Code review setting、PR comment/review thread、`docs/PRODUCT_REQUIREMENTS.md`、`docs/REGRESSION_PREVENTION_POLICY.md` | comment時head SHAとreview commit SHAの一致、GraphQL review threadのoutdated/resolved/severity監査、workflow secret/action scan | verified |
