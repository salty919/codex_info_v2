<!-- Copyright (C) 2026 salty919 -->
<!-- SPDX-License-Identifier: GPL-3.0-only -->

> **文書の位置づけ:** 本書は製品要件の非規範的な案内／実装説明です。要求の入口とowner registryは `docs/PRODUCT_REQUIREMENTS.md` および同文書から参照される REST/DATA/UX/LOCALIZATION 仕様です。要求変更時は該当する master ID と owner に従い、本書だけで契約や監査成果物を追加・変更しません。

# Codex Info UI 設計仕様

## 設計原則

内部のapp-server command（`thread/list`、`rollout`、`account/read`等）は収集手段であり、公開契約や要求ownerではありません。公開値は、`docs/PRODUCT_REQUIREMENTS.md`から参照されるREST仕様の現行`/v1/details` generationから導出します。

Linux / WSL のネイティブ UI は Slint の宣言的コンポーネントで構成する。常駐serviceだけがquota、local usage、historyを収集・正規化・保存するwriter責務であり、UI processはCodex app-server、session JSONL、履歴SQLiteへ直接到達しない。`windows-client/` の Windows 監視クライアントは別プロセスの Avalonia UI である。Linux / WindowsのMain、Graph、Threadsはいずれもstrict validation済みの単一`GET /v1/details` generationだけを表示rootとして消費し、値の再収集、再計算、行mergeを行わない。UI固有コードは同じrootのpresentation projectionと入力に限定し、両者は相手のUI stateへ直接アクセスしない。

- デザイントークン: `ui/theme.slint`
- 再利用部品: `ui/components.slint`
- 画面レイアウト: `ui/app.slint`
- フォント: `assets/NotoSansJP.ttf`と`assets/NotoSansKR.otf`をSlintへ埋め込み、起動時localeに対応するフォントを各Windowへ適用する

Windows版は次の依存方向を固定する。`Core`はREST契約の取得・検証、`Settings`は永続化と現行設定generation、`ViewModels`は状態遷移と表示意味論、`Graphing`は軸・tick・endpoint等の純粋計算を所有する。`Infrastructure`、`*.axaml`、`*.axaml.cs`、`Controls`はOS process、入力、描画を上位ロジックへ接着するadapterに限定し、設定保存、setup完了判定、graph座標・ラベル配置の独自計算を持たない。単体テストは前者を、binding・DPI・window lifecycle・実process・実描画は同一Windows成果物のE2Eを合格根拠とする。

Windowsグラフの表示参照元はX版の意味論と`Graphing`の純粋projectionである。0値と最大値は描画域の1%/99%へ置き、右端には系列線とは別のendpoint label gutterを確保する。gutterの物理幅は初期940×640 logical表示時の値をドル／トークンごとに保持し、横リサイズで増減する幅はplotへ割り当てる。モデル系列は停滞1px・増加3pxへ分離し、初回観測前を斜線で推測しない。最小plot幅で1px未満になるidle帯は描かず、ScottPlotの高密度`SignalXY`列集約は使用しない。adapterはprojection済み`Scatter`、限定grid、leader、labelを適用するだけとする。

余白・色・文字サイズを変更する場合は`theme.slint`の値を変更する。部品の配置を変更する場合は`app.slint`のレイアウト構造を変更し、Rustに座標値を追加しない。

## 情報階層

1. 残り利用枠（最重要）
2. リセットまでの残り時間
3. 週次または月間の対象期間の残り時間と7セルゲージ
4. 最新の実行中スレッド概要（モデル別件数）
5. 週次または月間の対象期間における、このマシンのモデル別利用履歴（存在する場合）
6. 警告・エラー・再試行
7. グラフウインドウの時系列（グラフを開いた場合）

残り時間は「6日と9時間12分」のように日数を独立した先頭単位として表示する。残り時間の意味を秒の変化で埋めず、日・時・分で読み取れることを優先する。

## 情報の所有権と重複防止

同じ事実を複数の部品が独立して表示してはならない。各情報には画面上の所有者を1つだけ定め、別の部品には同じ意味の言い換えも置かない。

| 事実 | 表示する所有者 | 表示規則 |
| --- | --- | --- |
| 更新・取得状態 | StatusBanner（未認証時はAuthPanel） | Headerには自動更新・確認中の補助文言を置かず、状態は下部のStatusBannerだけが所有する |
| Windows新版の通知と利用者開始操作 | Windows MainのStatusBanner右側 | 新版がある時だけ`vX.Y.Z を利用できます`と`更新する`を表示する。起動時確認は通知までで、download/Setup起動は利用者操作後だけ。常設Header操作、自動install、受信時刻との同時重複表示はしない |
| 最後に取得できた時刻 | StatusBanner | 「最終更新 HH:MM」を表示する |
| Windowタイトル領域 | 各Windowの自前タイトル領域 | ネイティブタイトルバーは使わない。ボタン以外の画面領域を移動に使い、画面内見出しは起動時localeで用途を示す。埋め込みフォントの操作記号で最小化・閉じるを提供し、Graphだけ最大化／復元と枠リサイズを提供する |
| アプリ内主見出し | Header | 「利用状況」を1回だけ表示する。OSタイトルを再掲しない |
| 期間ゲージの残り時間 | WeekGauge | 週次または月間の「リセットまで」をここだけに表示する。開始・終了の期間labelは表示せず、月間時も期間比率を7分割した7セルで表示する。青（単一accent）は`reset_at - now`を`window_seconds`で割った残り期間比率、未充填（単一neutral）は経過分であり、利用枠の残量や時系列位置を示すゲージではない。セル間は4pxの背景間隔とし、部分セルも同じ2色だけで描画する |
| 利用枠の残量 | RemainingQuota | 固定上限がある利用枠は残量%とバーを表示し、上限なしは見出し「利用枠」と主値「固定上限なし」を表示する |
| プラン・email | Account snapshot | schema検証済みemailとexact PlanTypeを内部snapshotで保持し、表示値の判定にだけ使う |
| 認証要求とlogin失敗 | AuthPanel | AuthRequired時だけ認証操作と固定の認証状態文をここに表示する。StatusBanner/Headerへ同義文を出さず、生errorを表示しない |
| 現在の実行中スレッド概要 | AccountActivity | 2行目にrunning件数と、モデル分類（SOL/TERRA/LUNA/その他）ごとの件数を表示する。集計token・先頭threadのモデル/タイトルは概要へ置かない。非実行時は「実行中のスレッドなし」だけを表示する |
| Linux / WSL版の現在の実行中スレッド全件 | Slint ThreadsWindow | canonical active snapshotの取得・dedup順は`updatedAt desc, id desc`を維持する。Threadsはroot/siblingを同順で親先行depth-firstへ投影し、各active parentのsubtreeを連続させる。行にはvalidated `is_subagent`由来の`メイン`/`サブ Dn`、depth guide、タイトル、モデル、親タイトル、`稼働`、`指示`、累積token、同じtoken eventの`last_token_usage.total_tokens`と`model_context_window`から算出したコンテキスト使用率／上限（`使用率% / 上限トークン`）を表示する。取得できない時刻や使用率は`—`とする。画面高を超える全件を固定Window内の縦スクロールだけで取得する旧導線は廃止し、採用しない。参照元`UX-20260822-UX-002`に従い、0〜3件の主要情報・主要操作は同一viewportに置き、4件目以降はページング・選択詳細・折りたたみ等で分割する。active snapshotの取得・dedup・presentation意味論は維持する。ThreadsWindowはactive snapshotを表示する |
| Windows版の現在の実行中スレッド全件 | Avalonia ThreadsWindow | 900×480 logical clientを拡大せず、空白を削った2行カードで先頭6件を同一viewportへ完全表示する。カード高56px、カード間隔4pxを固定し、7件目以降だけ一覧内の縦スクロールを許可する。文字を縮小して件数を稼がず、カードを1件表示時だけ拡大しない。親子railは64pxの専用gutter内に閉じ、本文・親タイトルと重ねない。取得・dedup・parent-first projection、role、model、context、token、経過、指示年齢の意味論はLinux / WSL版と共通にする |
| スレッドモデル分類 | AccountActivity | validated model labelをcase-foldして非英数字で分割し、既知tokenがちょうど1つのときだけSOL/TERRA/LUNAへ分類する。それ以外は`その他`。active threadがある場合は3分類の0件も表示する |
| スレッド経過・親子関係 | ThreadsWindow | `稼働`はvalidated `createdAt`、`指示`はrolloutのtimestamp付きuser message（`event_msg.user_message`または`response_item`のuser message）の最新値とUTC nowの差を短い日本語で表示する。時刻がない場合は推測せず`—`とする。active edgeから得たforest depthを表示depthの参照元とし、親先行depth-first・subtree-contiguousの順序、depth・tree guide・親タイトルを同じ行へ置く。native DB/rollout収集段階のcycle・dangling edge・partial sourceはcycle全体を拒否する。一方、RESTで完全に受理したthreads集合内に非null parent IDがない行はvalid orphanであり、SUB roleを保持して`親スレッドは現在非実行`を表示し、別rootへ接続しない。presentation検証でcycle・duplicate・schema不正を検出した場合も部分fallbackを公開せずcandidate全体を拒否し、last-good complete forestを保持する |
| 現在利用枠期間の概算ドル合計 | RemainingQuota | 主値行の右端に、同じ週次／月間期間のローカルSOL/TERRA/LUNAログ合計を整数丸めの「概算 $…」で1回だけ表示する。Graphの過去期間選択、請求額、クレジット残高とは連動させない |
| このマシンのモデル別履歴 | ModelUsage | Headerが示す現在の週次または月間期間におけるSOL/TERRA/LUNAだけを表示し、部品内で期間を再掲しない。見出しはモデル・入力・キャッシュ・出力とし、各カテゴリをトークン列と予想ドル列（整数部のみ）に分離して、それぞれ右揃えで表示 |
| グラフを開く操作 | Header | 認証済み画面の「グラフ」ボタンだけに置く。利用状況の数値はここへ再掲しない |
| 法的通知を開く操作 | Header / LegalNoticeWindow | Headerの「法的通知」ボタンだけが開く操作を所有し、GPLの無保証条項と第三者ライセンスの案内はLegalNoticeWindowだけに表示する |
| 法的通知Windowのタイトル | LegalNoticeWindow | 自前タイトル領域の「法的通知」と操作記号を表示し、法的意味の本文はウィンドウ内本文だけが所有する |
| 残り利用枠・SOL/TERRA/LUNAの時系列 | GraphWindow | 1つのグラフウインドウで残り利用枠・LUNA/TERRA/SOLの表示／非表示を個別に切り替える。初期状態は全系列を表示し、非表示系列は色とラベルのコントラストを下げて示す。各モデルは入力・キャッシュ・出力の累積値を1系列とする。右端値はプロット終端と系列色のリーダー線で対応付ける |
| グラフの対象期間と軸 | GraphWindow | 期間行は期間ラベル、プロット内はメトリック説明と「リセット直後（0） → 現在時刻」を表示する。ドル軸は表示中のモデル、token軸は期間内全モデルの共通値域で計算する |
| 履歴期間選択 | GraphWindow | DBに保存された3暦月分の実リセット境界を期間リストで所有する。相対的な「古い期間」「新しい期間」ボタンは置かず、途中リセットも別項目にする |
| グラフの表示単位 | GraphWindow | 「利用状況の推移」の右側の選択リストがドル／トークンを切り替え、系列toggleが表示／非表示を切り替える |
| グラフの利用変化 | GraphWindow | 各モデル系列を停滞区間1px（低い不透明度）と増加区間3pxに分け、同じ端点で接続する。service/detailsはlogical sampleを合成せず、UIも同一minuteをmerge/max/last/null化しない。ただしGraphの表示projectionだけは、confirmed gapでない前後の実測残量に挟まれ、モデル累積値が進んだactive区間へ既存の遅延残量補間を行い、次の実測残量がない終端は最後の実測値を保持する。導出した描画点をdetailsやDBへ書き戻さない。confirmed gapは不連続marker、全モデル累積値が不変な区間は専用の薄い未使用帯とし、線色はモデル所有色を使う |
| モデル別履歴の対象期間 | Header | 現在の週次または月間の対象期間を期間行へ1回だけ表示する。ModelUsage内やWeekGaugeの具体的なリセット時刻を再掲しない |
| 固定UI文言と言語 | 起動時I18nカタログと各画面の`UiStrings` | `LC_ALL`→`LC_MESSAGES`→`LANG`で選んだ10言語の固定文言を表示し、入力タイトル・メール・モデル名・製品名・ライセンス名・ログ生値は原文を表示する |
| 絶対時刻・期間ラベル | 起動時IANA timezoneを保持するI18n | ホストtimezoneの数値offset付き日時を表示し、経過・残り時間はUTC差分を言語単位へ変換する。無効timestampは値欄`—`、軸/期間は空または候補除外 |
| Windows クライアントの接続状態 | `windows-client` MainWindow の状態帯 | APIの `ready` / `initializing` / `auth_required` / `error`、通信障害、応答障害のうち1つだけを状態帯に表示する。`ready` のときだけ残量2%以下の危険、10%以下の警告、リセット24時間以内の警告をこの順で派生し、同じ状態帯だけが所有する。HTTP例外やJSON本文を出さず、stale時だけ前回受信値であることを状態帯に付記する。 |
| Windows クライアントの利用枠 | `windows-client` MainWindow の残り利用枠 card | `quota` がある場合だけ残量%とbarを表示し、null時は「未取得」と表示する。リセット時刻は補助cardだけが所有し、残量cardへ重複させない。 |
| Windows クライアントのモデル利用量 | `windows-client` MainWindow のモデル別利用量 card | `models` 配列のSOL/TERRA/LUNAだけを入力・キャッシュ入力・出力で表示する。空配列は同じcardの「未取得」だけを表示する。 |
| Windows detailsの概算合計 | `windows-client` MainWindow のRemainingQuota | `estimated_cost_label`は同じaccepted details rootのquotaがある場合だけRemainingQuota右側へ1回表示する。Graph、ModelUsage、Status、Headerへ複製せず、null/invalid時に値を推測しない。 |
| Windows接続profile・保存selector | Setup / Settings のConnection recovery | 設定JSONは`language`、`setupCompleted`、`connectionConfigured`、`timeZoneId`、`connectionProfile`、`connectionSelector`の6 keyだけを保存する。profile enumは`none`、`wsl`、`sshConfigAlias`のいずれか、selectorはWSL exact distribution tokenまたはliteral OpenSSH Host alias grammarだけとし、秘密・展開値・raw host/user/pathを保存しない。saved selectorのauto reconnectはArgumentList＋`BatchMode=yes`を使う。接続後はhealth readinessとstrict detailsを別段階で確認し、認証開始・確認はcontrol-onlyとしてdetails rootへ混合しない。4-key recoveryはMain disconnected＋Settingsだけに限定する。製品判定は`PRODUCT_PENDING`、参照元は`UX-20260822-SSH-001`とする。 |
| Windows失敗のCause・Impact・Recovery | MainのStatusBanner、Setup/Settings内の該当step | 同時に1 failure classだけを、固定localization keyのCause/Impact/primary CTAとして表示する。primary CTAは1個で、raw errorを表示せず、状態ごとのroute/保持は`UX-20260823-ERROR-001`を参照元とする。 |
| Windows Help / Connection guide | Mainから到達するHelp information surface | server/API silent起動、daemon、WSL、remote SSH、API確認、認証、設定/復旧、update/uninstall、診断情報をpre-authored pageで所有する。Helpは独立top-level Windowではなく、同じ説明をSetup/Statusへ重複させない。 |

モデル別欄は単価版`LOCAL_ESTIMATE_V1_2026-08-14`による概算ドルを表示する。1M tokenあたり入力／cache入力／出力の単価は、SOLが$5/$0.50/$30、TERRAが$2/$0.20/$12、LUNAが$0.20/$0.02/$1.20である。ログの3カテゴリは独立したドル列へ表示し、同一の累積snapshotは差分0として集計する。

更新周期は内部仕様とし、画面上の各事実はこの表の所有者が1回表示する。

## データ契約

- 表示事実の収集参照元を常駐serviceへ固定する。service内部ではaccount/email/planは同じ`account/read`応答、quotaは同じ`account/rateLimits/read`応答のcanonical object、thread一覧/ID/title/`createdAt`/`updatedAt`/pathは`thread/list`、task状態/model/tokenと最新user message timestampは選択threadのrollout、モデル利用量はvalidated local session JSONL、期間履歴はvalidated SQLite、概算単価は固定versioned constants、現在時刻は1回取得したUTC `now`である。別応答や別threadのfieldを一つのpublish candidateへ混合しない。Linux / Windows UIにとっての表示参照元は、この収集を完了してstrict validationを通過した単一`/v1/details` generationだけである。
- active threadのcanonical snapshotはvalidated candidateを`updatedAt desc, id desc`で保持する。この順序は取得・dedup・snapshotの参照元であり、Threads presentationだけがparent edge、validated role、forest depthを使ってroot/sibling sortと親先行depth-first projectionを行う。presentation reorderはsnapshotの追加・削除・liveness判定を行わない。
- 3暦月のcutoffは1回取得した現在UTC日時から月を3回減算し、対象月に同じ日がなければ月末へclampする。保存・表示対象は閉区間`cutoff <= timestamp <= now`で、未来・非正時刻・境界外rowを表示しない。sort/group/canonical IDはUTC秒で行い、期間listとグラフ横軸のUIラベルは起動時に解決したホストIANA timezoneへ変換する。
- 履歴のstorage scope、account/epoch境界、cycle admissionは、`docs/PRODUCT_REQUIREMENTS.md`から参照されるDATA仕様と`docs/LIVE_STATE_DECISION_MATRIX.md`に従う。DESIGNではlegacy DBを現行の公開scopeや分離済みaccountの代替として扱わない。cycleはquota値やcost値ではなく、既存の観測`timestamp/reset_at`列だけから決める。exact resetまたは既存許容幅内のjitterと、resetの前進量が観測時刻の前進量に追随するbounded rolling chainだけを同groupへ入れ、大きな同時刻jump、許容外の後退、追随しないdriftは別cycleとする。canonical reset/period IDはadmitted group内の最大`reset_at`、開始は最小timestampとし、同じminuteが複数groupへ属する候補は全体rejectする。残量100%、7日窓、quota-onlyなど値の形はadmission/除外条件に使わない。
- `HistoryCanonicalizer`は常駐service内だけに置く。同じprofile-owned DB scope、上記の同じcycle、同じminuteへ属するrowを一組とし、distinct non-null quotaが0または1個で、かつ既存のcumulative vector `(sol_dollars, terra_dollars, luna_dollars, sol_tokens, terra_tokens, luna_tokens)`のうち全rowをcomponentwiseに支配する値が存在する場合だけ、そのquota（0個なら`null`）とその既存dominant vectorから1 logical sampleを構築する。同値duplicateは同じvector値として冪等に扱う。quota競合、dominant vector不存在、非比較vector、scope/cycle/minute/period境界不明はcandidate全体をrejectしてlast-goodを保持し、component別max、row順のlast、null化、別rowの任意mergeで値を合成しない。local sessionの改行済みoversize/invalid-UTF8/JSON recordを隔離できるのは、同一fileの後続validated cumulative snapshotが対象列の欠落を覆うと独立に証明できるcaseだけとする。後続snapshotなし、usage eventか判定不能、I/O、path差替え、途中行、file/aggregate資源上限はfile/candidate単位で全rollbackし、前後の一部だけをDBへcommitしない。live rolloutの拒否単位は本項から継承せずL0を使う。
- ローカルセッション全走査はquota cycleの`Usage`受信時、明示的な更新操作、またはapp-server outage epochでpersisted `history/usage_reset_hint.json`とappend-only sourceが有効な場合のbackfill latch=1だけ実行する。fingerprint不変cycleのscan/writeは0、変化時も1 cycleにつき1 scan・1 transactionだけとする。thread/listの5秒周期はRPCとopen-FD集合だけを対象とし、履歴JSONLを走査しない。reset hintはschema `reset-hint-v1`、最大4KiB、`state=active|expired|tombstoned`、`reset_at`/`window_seconds`/`observed_at`/file cursor/個人識別値を含まないopaque `auth_epoch_nonce`を持ち、current AuthEpoch/nonceのfresh authenticated hintだけを受理する。`now >= reset_at`の旧hintは`expired`としてsource logを保持したままscan/新規DB rowを0にし、fresh authenticated hint（`reset_at > now`）受領後だけ新期間へ帰属する。quota cycle/明示更新で起動したscanもcurrent authenticated reset periodを検証できない旧hint・旧periodへ帰属させない。logout・token失効・AccountKey変更ではAuthEpochを先に増やし、persisted hintを`tombstoned`へatomic更新して別epochの公開を0にする。backfillは最大1 scan・最大1024 rowsかつ1MiBのtransactionで既存rowをrewriteしない。
- 期間listの基底labelは起動時ホストtimezoneの`YYYY/MM/DD HH:mm:ss ±HH:MM — YYYY/MM/DD HH:mm:ss ±HH:MM`とし、DSTを各timestampのoffsetへ反映する。同じ基底labelが複数groupに生じた場合だけ、各項目へcanonical reset時刻の期限suffixを付けて一意化する。有効なcurrent quota snapshotがあり、その`reset_at`とcanonical IDの絶対差が60秒以内かつ`now < canonical ID`である唯一のgroupだけ末尾へcurrent suffixを付ける。current quotaが欠損/無効ならどの項目にもsuffixを付けない。Rustは最終label→canonical IDの対応表を保持し、表示labelから日時をparseして期間を逆推定しない。GraphSelectは文字列への書込みではなく選択indexを同期し、旧期間から最新へ戻した際も最新canonical IDを再選択する。空ならlocaleの`No history`を1項目表示して選択を無効にする。
- `thread/list` requestは各pageで`sourceKinds=[cli,vscode,exec,appServer,subAgent,subAgentReview,subAgentCompact,subAgentThreadSpawn,subAgentOther,unknown]`、`sortKey="updated_at"`、`sortDirection="desc"`、`archived=false`、`limit=100`、`useStateDbOnly=false`を明示し、2 page目以降だけ直前の`nextCursor`を渡す。cwd/modelProviders/searchTerm/sectionIdは省略する。この10値は2026-08-14にCodex CLI 0.147.0から生成したv2 bundle SHA-256 `f3dec1e031d99a420b137b903f02196d4325eece57620c925bb7130b25f168d2`、`v2/ThreadListParams.json` SHA-256 `b227bb78acf9b91060d03c56d3f2072cdd9f1bd08290c11e8869f1a663b16da2`の外部contractである。hash/CLI版/取得UTC日をacceptance manifestへ保存し、値が変われば暗黙追従せずG2をFAILさせる。
- `thread/list`は履歴inventoryであり、新しく起動したapp-serverから見た`status=notLoaded`やrollout末尾の古い`task_started`だけでは別processの現在稼働を判定できない。対象platformのLinux/WSLでは、`comm`と実行file名がともに`codex`である`/proc/<pid>`を有限走査し、FD列挙の前後で`ProcessIdentity=(pid,starttime_ticks,exe_device,exe_inode)`が完全一致するeligible workloadだけから、canonical sessions root配下のregular JSONLについて`path -> nonempty owner identity set`を作る。exact Codex Info native/server artifactを祖先に持つ観測用`codex app-server` childは、別Codex Info processが生成したものを含めて除外する。祖先不明、identity変化、FD部分読取、observer reparent、上限超過はcycle全体をrejectし、pathだけの世代なし和集合や正常emptyへ丸めない。process最大65,536、1 processのFD最大16,384、open session最大1,024とする。canonical active snapshotの取得順は`updatedAt desc, id desc`を維持し、Threadsへの親先行投影は別段階で行う。
- Codex CLI 0.147.0の実測ではnative sub-agentが同じprocess内で動き、子rolloutを独立FDとして保持せず、新規app-serverの`thread/list`にも全sourceKinds指定で現れない。そこで、owner rootのvalidated thread IDだけをbind parameterとして`<CODEX_HOME>/state_5.sqlite`をread-only/query-onlyで読み、`thread_spawn_edges(parent_thread_id, child_thread_id)`を最大64階層・合計1,024子孫まで辿り、`threads`から子のID、rollout_path、updated_at、name/preview、thread_sourceを補完する。name/previewが空のnative childは、存在する`agent_path`の末尾を安全に人間可読化したタスク名、次に`agent_nickname`を表示名へ使い、固定文へフォールバックしない。DBのedge status、updated_at、recencyはlivenessに使わない。子は`thread_source='subagent'`、1〜128 scalarのID、有効timestamp、bounded title、sessions root内のnon-symlink regular rolloutを要求し、rootから到達しないtreeは除外する。cycle、dangling edge、schema/query failure、上限超過、DB/rollout差替えはcycle全体を失敗させ、親だけの部分snapshotを公開しない。
- owner rootと到達可能な子孫の各rolloutを同じcycle内でsecure-openし、同じeligible owner identity mapにpathがあり、最後のtask状態がrunningの行だけをcanonical active snapshotへ`updatedAt desc, id desc`で保持する。子完了は最後の`task_complete|task_completed|turn_aborted`で次回snapshotから除外する。live rolloutの改行済みoversize recordは、bounded streaming parserがduplicate/unknown envelope key 0の正規eventとliveness非変更tool payloadを完全証明した場合だけpayloadを隔離できる。invalid UTF-8/JSON/envelope、known state event型不正、証明不能oversize、EOF以外の部分行、inode差替え、symlink化、I/O、資源上限はcandidate/cycle全体をrejectし、古いtask状態をrunningへ流用しない。走査不能は旧一覧全体＋未確認を保持するthread-only failureであり、完全なeligible process/FD scanからopen owner root集合0件を得た場合だけ正常0件とする。Threads presentationはこのsnapshotを親先行depth-first・subtree-contiguousへ並べ替えるが、canonical snapshotやlivenessは変更しない。5秒ごとにthread一覧だけを再取得し、quotaの60秒更新を待たずにsub-agentの開始・完了を反映する。
- account/quota schemaの外部参照元は`protocol/SCHEMA_MANIFEST.json`とする。2026-08-14 UTCにCodex CLI 0.147.0へ`codex app-server generate-json-schema --out <empty-output-directory>`（`--experimental`なし）を実行したv2 bundle SHA-256は`f3dec1e031d99a420b137b903f02196d4325eece57620c925bb7130b25f168d2`である。生成raw SHA-256は`GetAccountResponse=dc770d8377dfe9b93b67e857e22ba7f99726265c8ca5cae63c5ad06e78b70b9d`、`GetAccountRateLimitsResponse=c67762e486f5a9b081ec42650fce657781669e6b1261c09c47bc8085d8cf2fc1`、bundleから`jq -cS .definitions.PlanType`で抽出したPlanTypeは`585f8053090425683e665307b22b7f2793e17f93340361bb1c00b95320d9643c`である。保存schemaは末尾LF差をraw hashへ混同せず、manifestのcanonical `jq -cS` hashでも生成物と一致確認する。CLI版、取得UTC日、生成command、experimental flag、bundle、raw/canonical/vendored hash、PlanType enum順のどれかが変われば暗黙追従を禁止してG2をFAILさせ、schema/要求/設計/security/全fixtureを更新後にG1〜G4と統合gateを再実行する。
- `thread/list` responseは、project意味判定より先に上記hashの生成`ThreadListResponse` schemaでpage envelopeと各`Thread`を検証する。必須の`data`、`Thread.cliVersion/createdAt/cwd/ephemeral/id/modelProvider/preview/sessionId/source/status/turns/updatedAt`、nullable field、union、enum、各型のどれかがschema不正なら、envelope不正はcycle-private accumulatorを全破棄して旧`ActiveThreadSnapshot`を保持し、itemだけの不正はそのitemをrejectする。schemaが許すadditional propertyはRPC全体size/depth/string上限内で受理するが、表示・選択・active判定へ使わない。
- **ライブ判定の不変条件（L0）**: DBの`threads`/`thread_spawn_edges`は履歴inventoryであり、livenessの証明ではない。rootとchildの全candidateは、同一cycleでsecure-openしたcanonical rollout pathがstable eligible workloadの`path -> owner ProcessIdentity set`に含まれ、rollout parserの最後のtask状態がrunningで、native owner/root/edge graphが完全である場合だけactiveとする。native DBのduplicate、root非到達、cycle、dangling edge、partial sourceはcycle全体をrejectする。完全受理済みREST PublicThread集合のmissing parentだけはpresentation orphanとしてSUB roleを保持し、native danglingから派生しない。publishは現行`(profile/account,SupervisorLeaseIdentity,CollectorEpoch,CycleSeq)`だけを受理し、別Codex Info collectorの旧lease/epoch/cycleはno-opとする。判定不能・identity変化・observer混入・cycle途中の部分結果は旧完全snapshot＋未確認を保持し、0件と成功値を混同しない。root/child/mixed/empty、workload/observer各複数、PID再利用、process停止・再起動、DB欠落・重複・edge破損、record failure、RPC失敗、stale admissionを`LIVE_STATE_DECISION_MATRIX.md`と同じ表形式fixtureで固定する。
- response itemの参照元はschemaの`source: SessionSource`である。valid形は文字列`cli/vscode/exec/appServer/unknown`、additional propertyを許さないobject`{"custom": <bounded string>}`、additional propertyを許さないobject`{"subAgent": "review"|"compact"|"memory_consolidation"}`、additional propertyを許さないouter object`{"subAgent":{"thread_spawn":{parent_thread_id:string,depth:i32,agent_nickname?:string|null,agent_path?:string|null,agent_role?:string|null}}}`、additional propertyを許さないobject`{"subAgent":{"other":<bounded string>}}`とする。current schemaの内側`thread_spawn`が許す追加fieldはboundedで受理して無視し、公開schema hashが変わればG2で停止する。native sub-agent objectを文字列でないという理由でrejectしない。union outerのunknown key、複数variant、禁止された追加property、required/type不正はitem rejectとする。`threadSource`はschema-validなstring/nullだけ、`status`はschema-valid runtime metadata、`turns`はschema-valid arrayであることを要求するが、active/model/tokenの参照元に使わない。候補は有効`updatedAt`降順、同値ならthread ID降順とし、全候補のrolloutを検査して最後のtask状態がrunningの全件を同順序で1 snapshotとして公開する。`thread_spawn`はvalidated `parent_thread_id`と`depth`を保持し、それ以外のsubAgent variantもsub-agent種別を保持する。updatedAt/path/id/sourceが欠損・型不正・安全でないitem、または同一cycle内の候補読取失敗はcycle全体をrejectし、他の候補だけの部分snapshotを公開しない。
- thread/listの表示titleはnormalized `name`が非emptyならそれ、なければrequired `preview`のnormalized非empty値、それも空なら固定`アクティブなスレッド`とする。native state DB supplementだけは、name/previewが空の場合にvalidated optional `agent_path`の末尾タスク名、次に`agent_nickname`を使う。schemaにない`title/rolloutPath/rollout_path/session_path/updated_at/threads/root array`へthread/listのfallbackを広げない。rollout pathはnullable schema field `path`だけを使う。
- rollout JSONLのtask参照元はtop-level `type=task_started|task_complete|task_completed|turn_aborted`または`type=event_msg`のobject `payload.type`同値、model参照元はtop-level/payloadの`turn_context|thread_context|thread_settings_applied`にある安全正規化後1〜128 scalarのmodel、累積token参照元は`payload.type=token_count`かつ`payload.info.total_token_usage.total_tokens`の非負u64である。コンテキスト上限は同じ`token_count.info.model_context_window`の非負u64、現在コンテキストtokenは同じeventの`info.last_token_usage.total_tokens`の非負u64として検証し、後者と上限の比率を使用率表示に使う。表示値は常に`使用率% / 上限トークン`の形で併記する。Codexが初期状態で出す`event_msg`の`token_count`（`info: null`）はトークン値を含まない安全なno-opとして受理し、`info`がobjectになった後の欠損・型不正・範囲外はそのcandidate file全体をrejectする。受理したmodel完全値を内部保持し、表示だけ24 scalar規則で短縮する。modelがnormalized空ならmodel eventなしと同じ`不明`、129 scalar以上ならknown event範囲不正である。最後の完全検証済みtask eventだけがrunningを決め、最後の完全検証済みmodel/token/context-windowだけを表示用snapshotへ採用する。未知eventは正規envelopeを完全検証した場合だけno-opにできる。open中rolloutはsecure-open時のdevice/inodeと長さをsnapshot境界とし、その境界以前の最後の改行までを有限streamで検証する。EOF直前の未改行tail（同じdevice/inodeへの追記を含む）はcandidate失敗にせず次cycleへ保留する。改行済みoversizeはstreamingで正規envelopeとliveness非変更tool payloadを証明した場合だけpayload隔離し、invalid UTF-8/JSON/envelope、known state event型不正、証明不能oversize、EOF以外の部分行、途中縮小、inode差替え、symlink化、資源上限超過はcandidate/cycle全体をrejectして、そのcycleからtask/model/token/context-windowを一切公開しない。
- 実行中threadの累積tokenはfile全体検証を通過したrollout中で最後に正常parseできる`payload.info.total_token_usage.total_tokens`の非負`u64`、コンテキスト使用率の分子は同じtoken eventの`info.last_token_usage.total_tokens`の非負`u64`だけを使う。`last_token_usage`が欠落するvalid fileではコンテキスト使用率を`—`とし、input、cached input、output、reasoningを合算して現在コンテキストtokenを推測しない。valid fileにtoken eventが欠落する場合はtoken欄を`—`で表示する。コンテキスト使用率は`last_token_usage.total_tokens / model_context_window`を小数1桁へ丸め、`使用率% / 上限トークン`として表示する。いずれかが欠落・上限0なら`—`とする。
- `protocol/SCHEMA_MANIFEST.json`のSHA-256 `585f8053090425683e665307b22b7f2793e17f93340361bb1c00b95320d9643c`を持つ`PlanType` exact enumを参照元とし、JSON stringをtrim/lowercase/区切り除去せずschema enumへ照合する。`PlanFamily`と表示labelは`free→無料`、`go→Go`、`plus→Plus`、`pro→Pro`、`prolite→Pro Lite`、`team→Team`、`self_serve_business_prolite/self_serve_business_usage_based/business→Business`、`ent26/enterprise_cbp_automation/enterprise_cbp_usage_based/enterprise→エンタープライズ`、`edu→教育`、valid enum `unknown→プラン未設定`とする。空、大小文字差、前後空白、区切り変更、非ASCII、未列挙、別型はschema-invalid responseであり、未知planへ変換して採用しない。月間判定はEnterprise familyの4 exact schema値だけで、schema外alias、prefix一致、Business包含一致は使わない。
- `account/read`はcycleの最初とquota取得後の再確認に使う。最初に生成raw SHA-256 `dc770d8377dfe9b93b67e857e22ba7f99726265c8ca5cae63c5ad06e78b70b9d`の`GetAccountResponse` schemaでresponse全体を検証し、required boolean、account nullable union、chatgptのrequired email/planType/discriminatorなどが不正なら全応答をrejectする。schemaが許すadditional propertyはboundedで受理して意味判定へ使わない。schema検証後、`account.type="chatgpt"`・valid nonempty email・PlanType exact enumが揃えば`requiresOpenaiAuth`の値にかかわらず認証済みとしてquota cycleを継続する。Codex CLI 0.147.0の実応答では認証済みChatGPT accountでも同flagがtrueだからであり、flag単独をlogout判定に使わない。usableなchatgpt accountがない場合だけ、flag=trueをAuthRequired、falseをunsupported/no-dataとする。schemaにaccount IDはないため`AccountKey`は完全なvalidated emailだけとし、最初/再確認のemailとPlanFamilyがともに一致しなければcycleをrejectする。
- quota source選択とfield validationは`VERIFICATION_PLAN.md`のQV/QDを唯一の決定表とする。意味判定の前に生成raw SHA-256 `c67762e486f5a9b081ec42650fce657781669e6b1261c09c47bc8085d8cf2fc1`のv2 `GetAccountRateLimitsResponse` schemaを応答全体へ適用する。required top-level、全nested required/type/enum/union、`rateLimitsByLimitId`の全entry、`rateLimitResetCredits`を含む任意present fieldのschema不正は全応答をrejectし、初回ErrorNoData/更新後last-valid保持とする。schemaが許すadditional propertyだけはboundedで受理して無視する。schema検証後、requiredのtop-level `rateLimits: RateLimitSnapshot`だけをcanonical objectとする。有効candidateなしはunavailableであり、`rateLimitsByLimitId`、matching limitId、key `codex`、model entry、wrapperなしrootへfallbackしない。canonical object内だけでEnterprise familyのvalid `individualLimit`、fixed primary/secondary、valid `credits.hasCredits:boolean`を伴う`credits.unlimited=true`の順に選ぶ。`hasCredits`の値、credits.balance、individual.limit/used、limitId、rateLimitReachedType、spendControlReachedは選択・remaining再計算へ使わない。fixed候補はwindowDurationMins降順、resetsAt降順、primary優先、field key辞書順とする。
- account/readとcanonical rateLimitsのPlanTypeが双方knownなら`PlanFamily`一致時だけquota cycleを継続する。rate plan欠損/null/`unknown`はaccount plan familyを参照元とし、双方known family不一致はcycle全体をrejectする。`rateLimitResetCredits`、`rateLimitsByLimitId`、unknown fieldは表示・保存・選択に使わず、それらの値・順・個数を変えてもcanonical出力を変えない。
- 選んだquota objectから`fixed_used_percent: Option<i32>/remaining_percent/reset_at/window_seconds/limit_name/monthly/unlimited`を1つのimmutable quotaへ構築し、account/email/plan、同期間のlocal model usage/costをまとめた`AccountQuotaSnapshot`にする。fixed候補だけは`fixed_used_percent=Some(used)`として`remaining_percent = 100 - used`を1回計算し、Enterprise individual候補は`fixed_used_percent=None`、schema-valid `remainingPercent`を直接使ってused/limitから再計算しない。account/quota/local usage/DBのいずれかが失敗したcycleは部分commitせず、初回はbar/percentを生成しないErrorNoData、有効値取得後は旧snapshot全体を保持したStaleErrorにする。ただしusableなchatgpt accountが消失してflag=trueになったAuthRequired、AccountKey変更、flag=falseのunsupported/no-data accountは旧account値の保持より優先して全account可視dataを空にする。認証済みchatgpt accountとflag=trueの組合せは消去経路へ入れない。
- account/read・account/rateLimits/read・account再確認だけを扱うaccount bridge、ローカルsession JSONLだけを扱うlocal bridge、thread/list・rolloutだけを扱うthread bridgeを、別worker・別channelとして起動する。account bridgeのapp-server child、stdin/stdout、RPC reader、要求ID、frame/noise budgetをthread bridgeと共有しない。account bridgeはlocal/thread commandを型として受け取らず、有効quotaを再確認後ただちにUI stateへ送る。local/threadの完了を待ってからquotaを送る順序は禁止する。account bridgeからErrorを1件受けた時点で、そのreceiverから同じpollで既にdrain済みの後続eventを全て破棄し、補助結果のepochを進めて既にqueue済みのthread/local結果もno-opにする。旧receiverをdropしてaccount bridgeだけを交換し、Error後の同一batchに残るUsage/Account/AuthUrl/Readyを新接続の値として再利用せず、自動の同一callback再試行も行わない。最後の有効な可視値とowner faultは保持し、threadの待機flagだけ解除して次の定期要求を可能にする。
- 内部のthread/list/rollout収集は順序付き`ActiveThreadsSnapshot`としてAccountQuotaSnapshotから独立取得する。各要求と結果に`AuthEpoch`を付け、現在の認証世代と一致する結果だけを反映する。thread cycleだけ失敗した場合は新AccountQuotaSnapshotをcommitできるが、旧thread一覧全体を保持してStatusをerrorへする。成功時は0件を含む一覧全体を1回で置換し、前回行との混在を許さない。逆にthread成功だけで失敗したAccountQuotaSnapshotのaccount/quota/cost/historyを部分更新しない。
- `SnapshotPublisher`は同じdetails candidateが持つ`reset_at`と`window_seconds`で切ったローカルmodel usageからMain用`estimated_cost_label`を計算する。週次・月間を混合せず、Graphの選択履歴とは独立させる。3カテゴリの非負有限額を合計した後にRustの`round()`を1回適用し、整数を3桁区切りにする。Mainは受理したlabelを表示するだけで再計算しない。
- Graph metric listは`["ドル", "トークン"]`の順、初期値`ドル`とする。期間listは上記group順をそのまま使い、選択はcanonical IDへ解決する。
- flat/rising分類は`HistoryCanonicalizer`とpublic candidate duplicate検査を通過した累積値へ適用する。同じcanonical period内で`next == current`をflat、`next > current`をrisingとし、同一timestamp重複や`next < current`をUIでlast/maxへ直さずdetails candidate全体をrejectしてlast-goodを保持する。座標はaccepted sample間を線形に結ぶだけで新しい値を生成せず、`RecorderGapLedger`で確定したgap、expired reset hint期間、tombstoned AuthEpoch区間はsubpath境界として線を橋渡ししない。軸対象最大値が0なら描画座標用の0〜1固定scaleを使う。全系列を非表示にした状態でもcontrol、plot枠、軸を表示し、model pathを空にする。

## 世代、応答採用、commit契約

- process内で`WorkerEpoch`、`AuthEpoch`、`ClockEpoch`、`CycleSeq`、`RequestId`を単調増加させる。`RequestId`はworkerごとの非負`u64`で再利用せず、`checked_add`失敗時は新規要求を送らずworkerを停止してerrorへ遷移する。`CycleKey = (WorkerEpoch, AuthEpoch, ClockEpoch, CycleSeq, AccountKey)`とする。現行schemaにaccount IDはないため`AccountKey`はChatGPT accountのvalidated nonempty email完全値だけをprocess memory内で使い、ログ・DB・画面へ追加表示しない。emailがなければcycleをrejectする。
- 各cycleは開始時のUTC`cycle_now`と単調時計deadlineを1回固定し、`account/read → account/rateLimits/read → account/read再確認`の順に実行する。最初と再確認の認証状態・AccountKeyが完全一致し、fixed quotaでは`reset_at > admission_now`であり、現在の`WorkerEpoch/AuthEpoch/CycleSeq`とも一致する場合だけcommit候補にする。quota期間が途中で切れた、logout、account切替、token失効、deadline超過、取消、worker終了のいずれかなら全候補を破棄する。
- JSON-RPC responseは、4MiB bytes、valid UTF-8、JSON depth 128、全object duplicate key 0を先に検査し、現在のoutstanding requestのID・期待stage・worker世代・deadlineが全て一致するときだけ1回採用する。ID不一致、duplicate response、timeout/cancel後、旧worker、旧auth、旧cycle、shutdown後、次cycle開始後のlate/out-of-order responseはtombstoneとして状態を変更せず破棄する。notificationとserver requestはRVでframe/sizeを検証した後、明示対応表にないmethodなら状態を変更せず破棄する。invalid UTF-8、oversize、partial frame/EOF、malformed/trailing JSON、over-depth、duplicate keyはstream境界を信頼できないworker protocol faultとして全outstanding requestをtombstone化しStopping→Reapedへ進め、同callbackではrespawnしない。frame境界を通過したvalid JSONのunexpected error/result組合せだけはcurrent requestを失敗させる。いずれも先勝ち/後勝ちまたは別requestのresponseへ読み替えない。
- refresh timerは単調時計を使う。sleep復帰やtick burstは`in_flight=1, pending<=1`へcoalesceする。commit直前の`admission_now`がcycle開始の`cycle_now`または最後に受理したUTCより小さければ、`ClockEpoch`を増加させcurrent cycleをtombstone化し、保存せずClockStaleとして旧snapshot全体を保持する。前進時は未来timestampを混入させず、quotaが`admission_now`時点で期限切れなら破棄して次の1 cycleをpendingにする。`admission_now`検査後はwall clockを使う判定を追加しない。DSTはUTC判定を変えずlabel変換だけへ影響する。
- SQLite transactionはstorage admissionを通過したraw履歴、`DataGeneration`、`DataHash`を常駐serviceだけがcommitする。失敗・busy・full・corrupt・migration errorはrollbackし、DBを削除・再生成しない。commit済みraw rowのread-only viewへ`HistoryCanonicalizer`を適用して完全なdetails rootと一つのimmutable generationを作る。Linux / WindowsのMain/Graph/Threadsはstrict validation済みdetails rootを1 callbackで置換し、個別field setter、別endpoint値、UI側収集結果を混ぜない。canonicalization/root構築/swap失敗は旧full rootを保持し、partial rootは公開しない。
- 履歴DBの読み書きは、`docs/PRODUCT_REQUIREMENTS.md`から参照されるDATA仕様が定める現行のstorage scopeに従う常駐serviceの実装責務とする。DESIGNはlegacy DB pathやpartition列を要求のownerとは扱わず、account分離の境界はDATA仕様と`docs/LIVE_STATE_DECISION_MATRIX.md`の現行値から導出する。timestampはminute-startである。`HistoryCanonicalizer`はpublic candidateを作るread-only段階でだけquotaとdominant vectorを1 logical sampleへ組み、SQLite rowをcomponent別`MAX`、coalesce、last-rowでrewriteしない。競合・duplicate・不明境界ではpublicationをrejectしてlast-goodを保持する。既存のraw SQLite rowはcanonicalization、reject、UI表示を理由にrewrite/deleteせず、DB削除・再生成・lock無視の上書きを行わない。次項のverified backup成功後に行う3暦月retention pruneだけを既存どおり唯一の通常破壊操作とする。account/epochの分離境界と回復状態は、`docs/PRODUCT_REQUIREMENTS.md`から参照されるDATA/LIVE仕様に従い、本書単独では判断しない。
- installed profileでは`$HOME/.local/bin/codex-info`が唯一の顧客control入口、user-systemdの
  `codex-info.service`が常駐daemon/loopback REST/recorderの唯一のsupervisorである。launcherの引数なし/
  `--start`/`--ui`は一つのRelease resolverとinstall journalでverified current generationへ収束し、
  source-bound health後だけ完了する。`--ui`は同世代payloadのX event loopだけを追加しwriter/listenerを増やさない。
  service・開発・E2E用payloadの引数なし/`--port PORT`は同一processでdaemon+RESTを所有し、addressは常に
  `127.0.0.1`だが、installed update/control 責務ではない。launcher `--stop`は同一bootの停止意図を記録して
  main serviceだけを止め、`--disable-autostart`はmain/timerを停止・無効化、`--remove`はunitだけを解除する。
  いずれもbinary generation、DB、backup、hint、gap/recorder/control state、source JSONLを保持する。
- installation stateはowner-only generation directory
  `<version>-<source_sha>-<manifest_sha256>`とatomic `current` symlinkを参照元とする。resolver/installerは
  L1 install lockだけ、runtimeはL2 recorder lockからL3 account writer lockの順だけを使用する。transaction journalを
  各phaseでatomic write/fsyncし、systemd activationは`--no-block`要求後にread-only pollする。source-bound healthは
  MainPID、starttime、exe device/inode/hash、profile lock、socket inode/FD、3-key healthの前後一致を要求する。
  `desired_state=running`のcompleted terminalはnew managed+healthyまたはold managed+healthyだけとする。
  `desired_state=stopped|disabled|removed`の操作は対応するservice/timer/unit状態、listener不在、保持対象、
  local generation整合をread-backした非稼働terminalだけを成功とし、unknown/foreign ownerはsignal/mutationなしの
  `SAFE_BLOCKED`とする。
- 起動時maintenanceはcanonical DB profileごとに1つの`MaintenanceOwner`だけが実行する。writer admissionを止めた同じ排他境界で、SQLite online-backup candidateのflush、`quick_check`/schema/row count/deterministic fingerprint/reset-period境界検証、verified rotation、3暦月prune transactionの順に行う。`.bak.1`は最新verified、`.bak.3`は最古verifiedとし、0/1/2世代から不足分を作成する。検証前candidate、writer競合、rotation失敗ではprune=0、現行DB・old memory・既存verified backupを保持する。
- schema変更が必要になった場合は、(1)旧schema startupをread/write拒否、(2)新しいmigration versionを別名DBへtransaction適用して全row/schema/一意key/件数/hash/期間境界を比較、(3)3世代backupを保持、(4)検証成功後だけatomic renameで切替、(5)失敗時は旧DB・backup・memory・UI/REST rootをそのまま使う、の3経路以外を許可しない。backup世代のrestoreは通常起動から自動実行せず、全writer/API/UI停止→現DB別名退避→最新完全verified世代のquick_check/schema/row/hash/period監査→atomic replace→reload/pair検証の明示操作だけを許可する。
- SQLiteとX11 UIを二相原子的にcommitできるとは扱わない。DB成功後、UI event enqueue/apply前にMain close、X11消失、channel closureが起きた場合、DBの新DataGenerationは正当な永続履歴として残し、消滅したUIへ反映しない。Mainが生存してUI applyだけ失敗した場合は`UiResyncPending(target_root_revision, target_root_hash, attempted=false)`とし、画面は旧`UiRootSnapshot`全体を保持する。refresh、Graph open、selector、loginを含む次のUI操作時に、同じtargetへ未試行なら最大1回だけ再同期する。callback実行時にmemoryの最新RootRevision/RootHashを再読し、それを1回swapする。開始時targetより新しいrevisionがあれば古いtargetを捨てて最新だけを使う。swap直後にmemoryがさらに進んでいればpendingを新target/未試行へ更新し、その操作を実行せずStaleErrorとする。同じtargetへのswapが失敗したら`attempted=true`とし、予定refreshは再試行しない。新RootRevisionまたは明示的な利用者操作だけが新しい1試行を許可する。同一操作内のloop/retryは禁止する。認証済みsettled状態ではDBとmemory/visible durable部分のDataGeneration/DataHashが一致し、かつmemory/visibleのRootRevision/RootHashが一致する。AuthRequiredではvisible durable account部分が空であることを優先し、履歴DBとの一致を要求しない。
- logout、token失効、AccountKey変更は常に`AuthEpoch`を先に増加し、outstanding request、UiResyncPending target、旧account rootをtombstone化する。その後、account ID/email/plan、quota、remaining、model usage/cost、history/Graph data/period selection、threadを全て空にした新しいAuthRequired rootを1回だけ公開する。Graphのmetric/toggleというaccount非依存controlだけは保持できる。AuthRequired rootのdurable versionは`None`、durable hashはcanonical empty durable structの固定`EMPTY_DATA_HASH`とし、旧DBのDataGeneration/DataHashを写さない。RootRevisionは1増加しRootHashを再計算する。履歴DB自体は非破壊で保持するが認証中は画面からアクセスできない。認証消去rootのenqueue/applyがMain生存中に失敗した場合は旧account情報を表示し続けずcontrolled shutdownへ進む。通常のlast-valid保持やDB/UI再同期はこの経路を上書きしない。
- schema-valid `requiresOpenaiAuth=false`でもaccount null、apiKey、amazonBedrock、invalid emailというunsupported/no-dataへ変化した場合は、以前のaccount snapshotがあれば同様に`AuthEpoch`を先に増加し、pending/cycle/resync/旧account rootをtombstone化する。公開するのは上記と同じaccount可視field空・durable version `None`・`EMPTY_DATA_HASH`のClearedErrorNoData rootであり、AuthRequired/login UIへは遷移しない。Graphのaccount非依存controlと非破壊履歴DBだけを保持する。Main生存中のclear apply失敗はcontrolled shutdownとし、last-valid保持で旧account dataを見せ続けない。
- account cycleとthread cycleは独立に成功できるが、UIへはroot snapshotをmutex下で再構成して1 generationとして渡す。faultはcomponent ownerごとに保持し、account成功でthread faultを消したり、thread成功でaccount/DB/clock faultを消したりしない。Statusは次の完全全順序で最初のactive stateだけを1件表示する: `ShuttingDown（UIなし） > AuthRequired > ErrorNoData(account, quota, DBの順) > UiProjectionStale > AccountStale > QuotaStale > DbStale > ClockStale > GraphFaulted > ThreadStale > quota danger(<=2%) > quota warning(<=10%) > reset warning(24時間以内) > Normal`。固定文は順に、認証画面、`利用状況を取得できません`、`表示を更新できません`、`利用状況の更新に失敗しました`、`グラフを表示できません`、`スレッド情報の更新に失敗しました`、危険/警告/リセット警告、通常更新時刻とし、生errorを出さない。ownerの成功だけが自身のfaultを消す。

## Lifecycle・競合契約

- app-serverは`Absent → Spawning → Initializing → Ready → Stopping → Reaped`だけを進み、二重startをcoalesceする。executable解決、spawn、initialize write/read、15秒timeout、EOF、frame fault、Ready直後の初回Readの各失敗を別edgeにし、生成済みchildは必ずreapする。initialize中のschema-validなID不一致responseはoutstanding request単位で最大1024件までnoiseとして破棄し、期待IDを待ち続ける。ID不一致だけで15秒deadlineへ達した場合はtimeoutとしてrequestを失敗させてchildをstop/reapし、1025件目はworker protocol faultとして全outstanding requestをtombstone化してStopping→Reapedへ進める。ID不一致responseをinitialize成功または別requestのresponseへ読み替えず、同じcallback内で自動respawnしない。Ready後の予期しないexitはcurrent requestを失敗させてStaleError/ErrorNoDataとし、同じcallback内で自動再spawnしない。失敗したworkerは同じcallback内でrespawnせず、次の既存account refresh周期だけが新workerを最大1回開始できる。resident serviceはUIのcontrol bridgeからdata mutationを受けないため、この周期を一時障害・外部ログイン後の回復経路とし、追加の即時retry loopや`WorkerFaulted` latchを設けない。
- login clickは1 in-flightへcoalesceし、認証pollも1本にする。login開始ごとに`LoginEpoch`を1増加する。validated URLを1引数としてspawn APIが成功した時だけ同epochの`BrowserLaunchReceipt`を作る。receipt付きzero exitだけが「URL受渡し成功」であり、account/read確認pollへ進める。receiptの有無にかかわらずnonzero・signal・monotonic 10秒timeout、およびreceiptなしzeroは失敗で、openerをreapし、pollを開始せず、そのLoginEpochのrequestをtombstone化して、全account dataが空のAuthRequired rootとAuthPanel所有の固定generic login errorを1回公開する。receipt付きzero後もaccount/readで認証確認できるまでAuthRequiredとする。pollはmonotonic 120秒で必ず停止し、timeout時はAuthEpochとLoginEpochを1増加、poll/account-confirm requestをtombstone化、openerが残ればreap、automatic retry 0とし、同じ空AuthRequired root＋AuthPanel所有の固定generic login timeoutを1回公開する。次の明示login clickだけが新LoginEpochで再試行できる。logout、token失効、別AccountKey、login再入もAuthEpoch/LoginEpochを増加させ旧pending responseを破棄する。opener childは終了を回収し、期限超過時はこのprocessが生成したPIDだけをterminateしてreapする。
- local JSONLはquota公開後にlocal bridgeへ`(AuthEpoch, reset_at, window_seconds)`を渡して集計し、完了結果のAuthEpochとquota期間がともに現在値と一致する場合だけmodel usage/cost/historyへ反映する。旧認証・旧期間・logout後の結果はfault表示も含め無変更で破棄する。secure openした同一file descriptorから読み、open中のappend-only fileは読取開始時の完全行までをsnapshotとし、同一device/inodeへの追記と書込途中の末尾行は次cycleへ保留する。改行済みoversize/invalid-UTF8/malformed JSON recordを隔離できるのは、同一fileの後続validated cumulative snapshotが全対象列の欠落を覆うcaseだけである。後続snapshotなし、usage eventか判定不能、EOF以外の部分行、EOFで未完了のvalid/invalid UTF-8/oversize record、途中truncation、rotation、symlink化、行/file/aggregate上限超過、I/Oはlocal candidate全体をrollbackするがaccount bridgeを停止しない。ローカルusage集計が失敗しても有効なaccount/quota snapshotは独立してMainへ反映し、同じquota期間の最後の正常なmodel usageを保持する。正常値がなければ概算は`—`、model表は非表示とし、0 dollar/token sampleをDBまたはGraphへ作らない。root/file消失は空データとして扱うが、permission・symlink・unsafe owner/modeはerrorとする。SQLite busy/full/corrupt/schema mismatch/migration中断はtransaction rollbackし、旧DBまたは旧memory generationを保持する。
- 内部のthread/list収集は全page、cursor、candidateをcycle-private accumulatorだけへ蓄積し、`nextCursor=null`かつselected rollout検証完了まではActiveThreadSnapshotへ一切公開しない。最大32 page/1024 unique itemとし、cursorはbounded opaque string、空pageはnextCursorが新規なら継続する。同一cursor再出現、循環、上限到達、partial framing、timeout/cancel、RPC EOFはaccumulator全体を破棄してcycleを失敗させる。同一thread IDは有効`updatedAt`が最大のitemだけを残し、同値は後勝ちにせず完全field一致なら1件へdeduplicate、不一致ならそのIDをrejectする。途中の削除・権限拒否・rollout path変更、または同一cycle内の候補読取失敗はaccumulatorと選択結果全体を破棄し、旧threadを保持して部分snapshotを公開しない。auth switchはAuthEpoch更新と旧thread消去、late responseはno-opであり、通常error保持と混同しない。
- Graph lifecycleは`NotConstructed → Hidden → Visible → Hidden`で、最初のbuttonだけが1つのSlint Graph instanceとclient XIDを構築する。construction/show途中のrenderer/XID failureは直接`GraphFaulted`へ進む。ユーザーの`CloseRequested` handlerはdefault native destroyを取消し、同じclient XIDをX11 UnmapしてHiddenへ進めるため、user closeにexpected DestroyNotifyは存在しない。Hidden後も含めclient XIDへのDestroyNotifyは常にunexpectedで`GraphFaulted`とし、同processで破棄・再生成・retryせずGraph buttonを無効化してStatus errorにする。再openは同じinstance/XIDをMap/show/raiseする。WM reparentはclient XIDが存続する限りfaultではない。X connection断またはMain client XID消失は全UIが消滅したものとしてshutdownする。
- GraphがDB transaction中にopen/selector操作を受けた場合は最後のcommitted history generationだけを表示し、uncommitted rowを読まない。新generation後も選択canonical IDが存在すれば選択を保持し、prune/account切替で消えた場合はcurrent group、なければ最新group、どちらもなければ`履歴なし`へ決定的に移る。metric/toggleは保持する。Graph updateとclose/shutdownが競合した場合、Hidden/Faulted/ShuttingDownへの遷移がdata callbackより優先し、late callbackを無視する。
- Main/Graph close、Stop、channel close、app-server/browser単独exit、timer発火、DB transaction終了、UI callbackはすべてidempotent cleanup tokenを確認する。最初のMain closeは他のfailure handlerより先に`ShuttingDown`をCASで1回設定し、新規admission/timer/RPC/UI callbackを禁止し、pending login/browser/poll/RPCをtombstone化する。Spawning/Initializing/Readyのどこでもapp-serverをStopping→Reapedへ進め、同時に届くinitialize EOF/timeout等はStatusを変更しないlate no-opとする。in-flight DBはrollbackまたはcommitのどちらかで終了させてUI反映は行わない。app-server/browserへgraceful stop、期限後TERM、さらに期限後KILLをこのprocessが生成した検証済みPIDだけへ送り、全childをwaitする。終了後callback、二重close、既に終了したchildはno-opとし、orphan/window/timer/threadを0にする。
- JSON-RPCのnoise countはoutstanding request単位で0から始め、正常response/失敗/取消で破棄する。1025件目はprotocol faultとしてそのworkerの全outstanding requestをtombstone化し、Stopping→Reapedへ進め、同じcallbackでは再spawnしない。次のrefreshだけが新WorkerEpochを開始できる。

### REST snapshot publisher・read-only境界

- 引数なしまたは`codex_info --port PORT`はWindow非生成のresident serviceであり、quota/local/historyの唯一のwriter責務として`RecorderWorker`、`HistoryCanonicalizer`、`UsageStore`、`SnapshotPublisher`を所有する。`SnapshotPublisher`はcommit済みの完全な`DataGeneration/DataHash`と現行`(profile/account, SupervisorLeaseIdentity, CollectorEpoch, CycleSeq)`から完全detailsを一つのimmutable generationへ構築する。REST requestとLinux / Windows UIからSQLite、JSONL、app-server、認証URLへ直接到達しない。
- 表示consumerのatomic rootはstrict validation済みの一つのdetails応答だけである。schema/domain/size、現行admission tuple、`DataGeneration`、`RootHash`のいずれかが不正なcandidate、write-lock競合、stale lease/epoch/cycleではDB、memory、REST、UIを変更せず直前完全generationを保持する。REST wireにgeneration/version fieldを追加せず、detailsのpublished-generation markerだけをopaque identityとして使う。
- v1 RESTの成功応答は完全一致した`GET /v1/health`と`GET /v1/details`だけで、全responseが`Content-Type: application/json; charset=utf-8`、`Cache-Control: no-store`、header aggregate 8KiB以下である。固定bodyの`Content-Length`はUTF-8 body bytesと一致し、response header allowlistはこの2つと固定body時の`Content-Length`だけで、`Set-Cookie`/`Location`/`Content-Encoding`/`WWW-Authenticate`/proxy/authentication headerなど追加headerは返さない。既知pathへのHEAD/POST/PUT/PATCH/DELETE/OPTIONS等の全non-GETはJSON 405、未知・case-altered・末尾slash・query付きpathはJSON 404とし、いずれもSQLite transaction、WAL/SHM、migration、prune、backup、DB row/hash、published generationを変更しない。
- v1 history sampleの`timestamp`はminute-startであり、unique keyはpartitionを含む`(partition_id,reset_at,timestamp)`。timestamp不連続だけからgapを推測しない。history periodはgraph終端`end_at`とは別にcanonical境界`reset_at`を持ち、clientは`id`のparseや`end_at`からの推測を行わず、60秒のcanonical toleranceでsampleを所属させる。daemon停止等でsource cursorから回収不能と`recorder-gap-ledger-v1`が確定したminute範囲だけを、details contract revision `rest-v1-details-reset-at-20260823`の`history_gaps`へredacted projectionする。Graph/Windows presentationはconfirmed gapだけをmarker表示し、pending/rejected gapや通常欠測を混同しない。confirmed gapに対する残量・tokenの線形補間、旧値複製、現在時刻推測は行わない。

## レイアウト規則

以下のpixel値は、複数thread表示を受けて確定した論理pixel基準の固定Main/Threads 900×480と既存Graph layoutを基準に、固定/最小/拡大/無駄余白抑制/線幅差を有限検査する設計値である。900論理px幅の本文は840論理pxで、固定Windowへ追加した幅を全て情報領域へ渡す。実際のX11 client物理pxはOSのDPI倍率に従う。

- Window寸法はX11のclient content logical pixelsを指し、title bar/frameを含まない。WSLg/X11を対象platformとし、winitのscale factorをOS設定へ委譲する。Xft.dpiがあればそれを使い、なければXRandRのモニター寸法から倍率を算出する。検証は`xwininfo` client geometry、現在のscale factor、`WM_NORMAL_HINTS`を併用し、物理寸法が`round(logical × scale)`になることを確認する。
- MainWindowとThreadsWindowはclient 900×480 logical px固定（初期・最小・最大）とし、物理client寸法は現在のOS倍率に合わせる。モニター間の移動やDPI変更では、論理寸法を維持したまま物理寸法だけを変更する。
- 固定WindowのSlint min/max hintsとwinit non-resizable設定はlogical 900×480を宣言し、X11 `_NET_WM_STATE`の最大化/全画面をstate monitorで除去する。winit `Resized` event guardは現在のscale factorから算出した物理900×480の一致と最小化のzero-dimensionだけをPropagateし、他のnon-zero mismatchをPreventDefaultして同じcallback内でunderlying winit `request_inner_size`を一度だけ要求する。このevent-triggered requestは許可するが、timer/Slint `Window::set_size`による修復、非表示化、破棄・再生成、時間pollingによる再試行は行わない。
- GraphWindowはclient 940×640pxを初期サイズ、700×480pxを最小サイズ、上限なしとして通常操作のリサイズを許可する。四隅は28px角の対角リサイズ領域を辺より優先し、方向カーソル、hover tint、隅の小さなブラケットで操作位置を示す。ネイティブタイトルバーは使わず、ボタン以外の画面領域から移動し、画面内操作記号で最小化・最大化／復元・閉じるを操作する。preview指定は初期サイズのレイアウト検証用に使う。
- 本文は最大840pxで左右中央寄せし、900px幅では左右30pxを確保する。本文カードは同じ左端・右端を共有し、Headerのアカウント表示をタイトルと重ねない。
- 認証済み画面はHeader、RemainingQuota、WeekGauge、AccountActivity、ModelUsage、StatusBannerの順に縦積みする。各部品はそれぞれ48/96/56/52/100/36px、間隔8pxとし、全表示時は`48+96+56+52+100+36+8×5=428px`。上20px＋スタック428px＋内部安全余裕12px＋下20px＝480pxでMainWindowに収める。履歴なし時はModelUsageを生成しない。
- Mainの追加情報はカード高を増やさない。RemainingQuota内側幅800pxでは、概算ドル欄を右端216px・13px font、主値との間隔12px、主値側572pxとし、`u64::MAX` tokenから得る最大概算額も整数3桁区切りで収める。AccountActivityは総数を主値、SOL/TERRA/LUNA/その他を色付きのモデル別統計として配置し、`詳細`操作を最右64pxへ8px間隔で置く。Main概要ではthread tokenを集計しない。
- ThreadsWindowは左右30px・上下20pxの本文840×440px、header 48px、list 384pxとする。1件だけのときはrowを384pxへ拡張して詳細カードとして使い、タイトル28px、モデル22px、`稼働`/`指示`の値28px、コンテキスト使用率/上限・トークン値24pxで表示する。複数件のときはrow 128pxとし、384pxのviewportに3件を完全表示する。タイトル20px、モデル18px、経過・token・使用率18pxを保つ。4件目以降は`UX-20260822-UX-002`で確定したページング、選択詳細、折りたたみの段階表示で到達可能にする。同じviewport/ListViewの縦スクロールだけを到達手段とする旧導線は禁止する。ここではlist viewport 384px、row寸法、文字サイズ、tree rail等の既存レイアウト契約を変更しない。単一件の詳細カードでは、期間のラベルと値、`使用率% / 上限トークン`、使用トークンのラベルと値を余白を詰めて段組みし、親タイトルがない行はその帯を折り畳んで空の下段を作らない。`稼働`はcreatedAt基準、`指示`は最新user message基準で、事実がない場合は`—`とする。canonical snapshotのsortは`updatedAt desc, id desc`を維持するが、Threads presentationはrootとsiblingを`updatedAt desc, id desc`でグループ化し、親先行depth-first・subtree-contiguousへ投影する。roleはvalidated `is_subagent`だけから決め、falseは`メイン`、trueは`サブ`とする。connected rowのforest depthをDnへ使う。完全に受理した同一threads集合で非null `parent_thread_id`が存在しない行はvalid orphanとしてtop-levelのSUBへ投影し、正のvalidated depthがあれば保持し、なければplain `サブ`とする。別rootへ再接続したり親を捏造したりしない。cycle、duplicate ID、schema不正、またはpartial/rejected集合だけをpresentation前にcandidate全体rejectとし、last-good complete forestを保持する。validation後にcycleが残るという到達不能な防御分岐でも部分fallbackを公開せず、同じ全体rejectを適用する。複数行のtree railは本文左の専用gutter（base x=92px、depth step=20px）に置き、タイトル開始位置と同じdepth式から算出する。親から子への横枝はタイトル中心y=28pxで8px手前まで、親の縦幹は次の兄弟がある場合だけ行末まで延長し、depth 0〜3のguideを同じ座標系で描く。線は2px・warning 72% opacity、接続端は4pxのjunctionで固定し、relation文字やタイトルへ重ならない。表示depthは3で飽和し、深い木でも横幅を無制限に増やさない。
- Threadsのtree railはwarning色72% opacityの2px、複数件row height=128px、単一件row height=384px、depth step=20pxとする。display depthは`D=min(forest_depth,3)`、level `L=1..3`のx座標は`x(L)=92+(L-1)*20`。connected rowのcurrent vertical branchは`D>=1`で`x(D), y=0..28`、次siblingがあるときだけrow全高へ継続し、title中心の`y=27..29`へtitleの8px手前までhorizontal branchを描き、終端には4px junctionを置く。active childを持つrowは次levelのdownward railを`x(min(D+1,3)), y=28..row高`へ描き、rootはlevel 1を使う。required ancestor continuationはrow全高をspanする。connected title/parent textは`x=116+20D`、root/orphanはx=116から開始し、single-threadのdetail cardだけx=100から開始する。metadata列はindentに関係なく固定し、orphanはvertical/horizontal railを描かず、parent IDがあれば`親スレッドは現在非実行`だけを表示する。ListView clipはviewport端の部分rowだけをcropし、完全可視row内のrailをshift/terminateしない。
- ThreadsWindowの表示件数方針は、0件は空状態、1件は詳細カード1枚、2件以上は「最初の3件を完全に比較できる」ことを優先する。3件を超える場合もカード寸法を変えず、4件目以降は`UX-20260822-UX-002`のページング・選択詳細・折りたたみ等で分割する。同じListViewの縦スクロールだけで4件目以降へ到達する旧導線は禁止する。これは画面を一度見た時点で複数の実行を比較でき、かつ老眼向けの18px級本文を維持するための固定判断であり、行数に応じてフォントを縮小したり、カードを横方向へ押し広げたりしない。
- 直前2項のThreads寸法はLinux / WSL版Slintの参照元である。Windows版Avalonia ThreadsWindowは900×480 logical px固定のまま、外周20px、カード高56px、カード間隔4px、2行表示を使って6件を完全表示する。7件目以降だけ一覧内`ScrollViewer`で到達可能にし、Window拡大、本文font縮小、1件時の空疎な拡大card、railと文字の重なりで表示件数を作らない。
- 意図的elideを許すのはemailとthread titleだけとし、末尾`…`を必ず表示する。モデル完全値は内部で128 scalarまで保持し、R4のUIモデル名だけは描画前に24 scalar規則で末尾`…`へ決定的に短縮してから、268px欄へ省略せず全体を描画する。plan、短縮後のmodel表示label、token、概算額、割合、期間残り時間、モデル別数値列、Status固定文はUI部品内の追加elide/clipを許さない。全Textの描画boxは親box内に上下左右2px以上の安全域を持つ。email/title/modelの完全値は内部に保持し、生値を複数箇所へ再掲しない。
- `if`条件で状態ごとの部品だけを生成し、非表示部品がレイアウト空間を占有しないようにする。
- 7セルは各セル幅を`(カード幅 - 左右内余白40px - セル間隔24px)/7 = (カード幅-64px)/7`とし、セル間隔は4px（6箇所、合計24px）とする。
- 月間周期も7セルを月間期間の比率による7分割として使い、セル上段の残り時間だけを期間ゲージへ表示する。
- StatusBannerは固定短文を表示し、取得した生のJSON-RPCエラーを画面へ出さない。
- 900×480の本文領域（本文幅840px、左右30px）に通常・警告・エラー・zero/fullをスクロールなしで収め、すべてのカードを同じx=30〜870pxへ揃える。
- GraphWindowは940×640を初期サイズ、700×480を最小サイズとし、MainWindowとは独立して1つだけ開閉する。外側余白は`margin = 20px + (window-width - 700px) / 24`を20〜30pxへclampし、700px時20px、940px時30pxとする。previewのサイズ指定は初期サイズのレイアウト検証用に使う。
- Graphのタイトル行は高さ36pxとし、右端に自前操作108px、その左にmetric list 128px、間隔各8pxを置き、タイトル幅を`content-width - 252px`とする。期間履歴listはy=72px、高さ36px、本文幅、4系列toggleはy=144px、高さ24px、plot cardはy=176px、高さ`window-height - 224px`とする。期間popupはGraphSelectの前面レイヤーとしてplot上端へ重なり、開閉でtoggle・plotの位置と高さを変えない。項目が多い場合は`UX-20260822-UX-002`の分割原則（ページング・選択詳細・折りたたみ等）を使い、popup内ListViewの縦スクロールだけを到達手段にする旧導線は禁止する。受入用の自動展開はpreview専用で、通常起動時は閉じた状態を維持する。
- Graphの操作帯（期間listと系列toggle）は暗色surfaceの濃淡でplot cardと区別し、操作帯自体に外周枠を重ねない。plot cardだけを境界線で囲む。期間listのpopupは選択欄位置から前面へ重なって開き、2〜4件は全行を表示し、5件以上の選択肢は`UX-20260822-UX-002`の分割原則でページング・選択詳細・折りたたみへ分割する。行領域128px（4行）＋外枠2pxのpopup寸法上限、popup開閉で系列toggleとplot cardの位置・高さを変えないこと、popupがplot上端へ重なっても選択できることは既存レイアウト契約として維持する。内部ListViewの縦スクロールだけで5件目以降へ到達する旧導線は禁止する。metric listは標準の明色widgetを使わず、選択中・未選択・hover・open/closeを同じ暗色テーマのsurface/border/accentで表現し、選択値は常に「ドル」または「トークン」のいずれか一つにする。metric popupはタイトル行の左予約帯へ開き、期間list・系列toggle・plotを覆わない。履歴が空または取得エラーでも操作帯とplot cardの構造を維持し、plot内部には所有者が定めた「記録なし」または既存値だけを置く。
- Graph plotは左軸84px、plot開始92px、現在値欄（ドル80px／トークン112px）、leader gap 10px、右padding 4pxを予約し、ドル表示の`plot-width = content-width - 186px`とする。plot cardの高さを`window-height - 224px`、内部SVG pathの高さを`window-height - 276px`とする。700×480でplot幅474pxを確保する。現在値はplot内へclampし、近接ラベルは上下16px以上離して衝突を避け、終端からラベルへ系列色のleaderを描く。
- Graphの軸/current labelは予約欄内へ収め、0、999、1.0K、999.9K、1.0M、999.9M、1.0B、最大入力の各固定fixtureでclipしない。label同士の縦間隔は物理16px以上、plot上下端から物理8px以上とする。
- 残り利用枠とLUNA/TERRA/SOLの表示／非表示は4個の系列toggleが所有し、色とラベルのコントラストで状態を示す。ドル／トークンはタイトル右のmetric listで切り替える。残り利用枠はどちらのmetricでも独立した0〜100%系列のまま表示する。ドル軸は表示中のモデル系列の最大値、token軸は期間内の全モデル共通最大値を使う。グラフのJSON文字列はRust側で保持・解釈し、Slintへは0〜100のviewboxに変換したSVG Path commandとして渡す。
- 各モデルの累積系列はstrict detailsに含まれるcanonical値をそのまま使い、完全一致を停滞、増加をrisingへ分類する。停滞runは水平1px path（opacity 0.5）、増加runは線形3px path（opacity 0.95）にし、分類境界の同じ原値端点を共有する。round cap/join、fill透明、plot内clipとする。同一period内の減少、同一canonical minute重複、欠損・非有限はUIでmax/last/null化せずcandidate reject対象とし、last-good seriesを保持する。
- グラフは履歴の分単位の累積snapshotを時間軸へまとめ、SOL/TERRA/LUNAをそれぞれ独立した線形lineで表示する。ドル軸は表示中モデルの個別最大値、token軸は期間内全モデルの共通scaleを使い、各lineの座標と右端labelはモデル自身の値へ対応させる。対象最大値0は0〜1とする。横軸はリセット直後（0）を左、現在時刻を右に置き、記録がないときはplot領域に「記録なし」だけを表示する。

## 日本語タイポグラフィ

リポジトリ既存assetのNoto Sans JPとNoto Sans CJK KRをSlintへ埋め込み、localeに応じて`UiStrings.font-family`を各Windowへ適用する。主数値は48px、本文14px、注記13pxを基準とし、フォントのOFL-1.1通知は`THIRD_PARTY_NOTICES.md`と`assets/NOTICE.txt`に記載する。

## 状態

- 通常: 青のアクセントとニュートラルな状態バナー
- リセット前後24時間: アンバーの状態バナーと、わずかな警告色のキャンバス
- 利用枠10%以下: 警告、2%以下: 危険
- 取得失敗: 既存の数値を保持し、状態バナーだけをエラー表示
- 初回取得失敗: quota percentage/bar/countdown/model historyを生成せず、StatusBannerだけに固定文`利用状況を取得できません`を表示する。初期値0%/100%を有効値として見せない
- 認証切れ: 旧account/quota/thread表示を消し、認証画面だけを表示する
- DB破損/migration失敗: DBを削除・再生成せず、memoryに有効値があれば保持、なければ初回取得失敗と同じ表示にする

## 受入基準

- MainWindow/ThreadsWindow 900×480の本文左右余白は各30px、GraphWindowは700×480で各20px、940×640以上で各30pxとし、左右差0pxにする。
- 7セルの期間ゲージが枠外へ出ず、週次・月間とも端数が比例表示される。
- 6日以上残る状態を文言とゲージの両方で即座に理解できる。
- 通常・警告・エラーで部品の段差や下端クリップがない。
- canonical state ID `initializing/auth_required/normal/quota_warning/quota_danger/reset_warning/zero/full/api_error/transport_error/status_invalid/details_invalid/history_error/thread_error/db_error/stale/no_history` の17状態について、各surfaceでapplicableまたは理由付きtyped N/Aを割り当て、applicable cellではText box安全域2px、部品重なり0px、Main/Threads本文左右端x=30/870、下端<=460pxを実測する。`weekly/monthly`はperiod_type、`active/idle`はactivityという別data dimensionでありstate IDへ昇格しない。`unlimited`および旧alias `auth/warning/error/reset-warning/history-empty`を受入stateとして使用しない。
- 認証済み全表示時の算術上のスタック高は428px。上20px＋スタック428px＋内部安全余裕12px＋下20px＝480pxを満たし、これより短くしない。
- MainWindow/ThreadsWindowの本文は900px幅でx=30、幅840pxを厳密に維持する。GraphWindowは700×480以上・上限なしで通常操作のリサイズを許可し、自前タイトル領域から移動・最小化・最大化／復元・閉じるを操作する。
- 全Windowは`no-frame`でネイティブタイトルバーを無効にし、画面内見出しと操作記号を埋め込みフォントで描画する。Header/AccountActivityにemail/planまたは固定product titleを再掲しない。
- Threadsのcanonical active snapshot sort (`updatedAt desc, id desc`) とpresentation preorderを分離し、shuffled IDs/rolesでparent-first・subtree-contiguous、orphan/cycle規則、2px `#e6a23c` rail、20px depth step、指定x/y座標・viewport端の部分row clippingをpixel実測する。
- Windows Threadsは900×480 logical clientのfresh画像で6件の56pxカードが欠けず、6件時に縦scrollbarがなく、7件時に一覧内scrollだけが有効となり、rail・タイトル・親タイトルの重なりが0pxであることを実測する。
- Main/Threads/Legalはwinit `Resized` mismatch guard、`set_resizable(false)`、X11 state monitorで自前タイトル領域からの移動だけを許可する。Graphは`no-frame`と`set_resizable(true)`、`resize-border-width`、四辺・四隅の自前ヒット領域からのリサイズを許可し、X11/WSLgではpointer追跡によるconfigureを使い、他のbackendではwinit `drag_resize_window`へフォールバックする。固定サイズguardと最大化・全画面除去の対象にしない。固定Windowのmismatch時だけevent-triggered `request_inner_size`を許可し、timer/Slint `set_size`修復・hide/recreateは0件とする。
- Graph以外のMain/Threadsの情報面は外周の二重枠を設けず、背景濃淡・余白・見出し・1px区切り線で階層を示す。Graphはplot cardの枠を唯一の主境界として保持する。Graphの最大化／復元は現在モニターの物理サイズ・位置へ直接適用し、WSLg/X11の仮想ルート全体を描画対象にしない。
- Graphは700×480/940×640/1200×800/maximized/fullscreenの全てで外側margin<=30px、plot外clip 0、label間隔>=16px、plot上下安全域>=8pxを満たす。
- 完全な変更pathから `scripts/quality_plan.py` が選ぶmaster ID直接オラクルだけが通る。
  この派生文書を根拠に、全build、全test、clippy、実画面を一律に追加しない。
